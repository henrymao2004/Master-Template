\documentclass{article}
\usepackage{iclr2026_conference,times}

\input{math_commands.tex}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{array}
\usepackage{xcolor}
\usepackage{colortbl}
\usepackage{hyperref}
\usepackage{url}
\usepackage{xspace}

\hypersetup{
  colorlinks=true,
  linkcolor=black,
  citecolor=blue,
  urlcolor=blue
}

\definecolor{lightblue}{RGB}{232,244,251}
\definecolor{lightgray}{gray}{0.94}

\newcommand{\gym}{\textsc{SkillMisevo-Gym}\xspace}
\newcommand{\bench}{\textsc{SkillMisevo-Bench}\xspace}
\newcommand{\safeevolve}{\textsc{SafeEvolve}\xspace}
\newcommand{\evolution}{\mathcal{E}}
\newcommand{\library}{\mathcal{L}}
\newcommand{\taskseq}{\mathcal{Q}}
\newcommand{\asr}{\mathrm{ASR}}
\newcommand{\carry}{\mathrm{CarryHarm}}
\newcommand{\ccmini}{Claude Code / MiniMax}
\newcommand{\codexmini}{Codex / MiniMax}
\newcommand{\hermesmini}{Hermes / MiniMax}
\newcommand{\openclawmini}{OpenClaw / MiniMax}
\newcommand{\cchaiku}{Claude Code / Haiku}
\newcommand{\codexgpt}{Codex / GPT-5.4-mini}

\title{Practice Makes Unsafe: Skill Misevolution in Self-Improving LLM Agents}

% Keep authors anonymous for submission. Replace only for camera-ready.
\author{Anonymous Authors}

% \iclrfinalcopy % Uncomment only for the camera-ready version.

\begin{document}
\maketitle

\begin{abstract}
LLM agents increasingly improve themselves by distilling successful
interactions into reusable skills. This turns experience into persistent
behavioral policy, yet existing evaluations measure whether evolution improves
capability without testing what happens when success depends on an unsafe
shortcut. We identify \emph{skill misevolution}: a transient unsafe success is
extracted into an apparently legitimate skill and later reused on benign tasks
after the original attack has disappeared. We introduce \gym, a
task-source-agnostic environment that turns executable agent tasks into stateful
skill-learning episodes. It standardizes skill writing, versioned library
state, retrieval, replay, and fresh-session evaluation across agent frameworks
and evolution methods. On top of this environment, \bench defines an
exposure-to-carryover protocol, paired evolution controls, and metrics for the
evolved artifact and post-attack behavior. We instantiate the benchmark with
AgentHazard as its executable safety-task source. Across four agent
frameworks, three model backbones, six target-agent configurations, [M]
evolution methods and ten AgentHazard risk categories, evolved artifacts reach
[$S_{\mathrm{content}}$/$S_{\mathrm{general}}$/$S_{\mathrm{stealth}}$] on
content unsafety, unsafe generalization, and stealthiness; post-attack
carryover harm reaches [H] while carryover utility remains [U]. Unsafe
content appears in the skill library before it consistently manifests as
behavior, and as few as [K] exposures produce harm that survives into a clean
session. We further introduce \safeevolve, a governance layer that audits and
repairs candidate skills, preserves verification steps, tracks provenance, and
retires skills with unsafe reuse histories. Under \safeevolve, the artifact
scores are [$S'_{\mathrm{content}}$/$S'_{\mathrm{general}}$/$S'_{\mathrm{stealth}}$],
carryover harm is [H'], and carryover utility is [U'].
These results show that self-improvement is a security-relevant policy update:
practice can make an agent more capable and persistently less safe.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

LLM agents are beginning to improve through use. Modern CLI and coding agents
can summarize successful trajectories, write or revise reusable skills, and
retrieve those skills when related tasks appear. This capability changes an
agent from a fixed policy with transient context into a system whose behavior is
partly rewritten by its own experience. Skill evolution is therefore more than
memory: it is a persistent policy update expressed as an editable artifact.

The signal used to produce that update is usually task success or efficiency.
Successful behavior, however, is not necessarily safe behavior. An agent may
complete a task by skipping a verification step, assuming authorization,
treating an irreversible action as routine, or following an attacker-controlled
observation. If the evolution method compresses that trajectory into a reusable
procedure, it can preserve the unsafe shortcut precisely because the shortcut
helped the agent succeed.

This creates a temporal safety failure that independent-task evaluations cannot
observe. The original attack may disappear with the context window, but the
lesson extracted from it can remain in the skill library. A later benign task
can retrieve the skill, apply its over-generalized rule, and reproduce the harm
without a malicious instruction. A fresh session can inherit the same behavior
by loading the evolved library alone. We call this process \emph{skill
misevolution}: transient unsafe success becomes persistent, retrievable, and
generalizable policy.

Measuring misevolution requires an environment that exposes persistent learning
as experimental state. We introduce \gym, which wraps executable agent tasks
with a standardized skill lifecycle: it snapshots the
library before and after each task, resets conversational and workspace state,
records skill writes and retrievals, and mounts a frozen library in a new
executor session. Only the evolving library crosses task boundaries.

\bench is the evaluation protocol built on this gym. Its basic unit is a
seven-task episode in which benign tasks and adversarial exposures from a
configured task source alternate, followed by a persistence probe in a fresh
executor session. Matched no-evolution,
read-only-reflection, and benign-evolution controls isolate whether later harm
comes from the learned artifact rather than the base model, repeated context,
or ordinary benign learning.

The benchmark evaluates three stages of the failure. During attack, it compares
malicious ASR on exposed tasks. At the evolved-artifact stage, it measures
content unsafety, unsafe generalization, and stealthiness. Post-attack, it
measures carryover harm and carryover utility in a clean session. This
distinction matters because an unsafe skill can remain latent
until a later task supplies the right retrieval cue. Behavior-only evaluation
therefore misses risk that has already entered the persistent policy.

Our evaluation asks four questions. \textbf{RQ1} tests whether evolution
increases unsafe behavior while retaining utility. \textbf{RQ2} follows the
exposure-to-persistence chain and tests whether the skill artifact mediates
later harm. \textbf{RQ3} measures how the effect varies across evolution
methods, agent configurations, task categories, attack strategies, and attacker
budgets. \textbf{RQ4}
tests whether \safeevolve can break this chain without removing the capability
benefits of learning.

\safeevolve acts at the learning boundary rather than as another runtime
refusal layer. It audits the complete candidate skill bundle before admission,
repairs unsafe generalization instead of deleting useful procedures, preserves
verification requirements during revision, records the experiences from which
each skill was learned, and downweights or retires skills whose reuse produces
unsafe behavior. These components target the persistent object responsible for
the incremental risk: the evolved skill library.

Our contributions are:
\begin{enumerate}
  \item We identify \textbf{skill misevolution}, a temporal failure mode in
  which self-improvement converts transient unsafe success into persistent
  behavior that can reappear on benign tasks without the original attack.

  \item We introduce \gym, a reusable environment for safety evaluation of
  self-improving agents. It provides a common skill lifecycle, versioned library
  state, isolation and replay, retrieval traces, and adapters for multiple agent
  frameworks and evolution methods.

  \item We introduce \bench, a task-source-independent temporal protocol with
  paired evolution controls and metrics for content unsafety, unsafe
  generalization, stealthiness, carryover harm, and carryover utility. We
  instantiate it with AgentHazard as the executable task and judge source.

  \item We provide a systematic evaluation across four agent frameworks, three
  model backbones, six target-agent configurations, [M] evolution methods, ten
  AgentHazard risk categories, and multiple exposure budgets, separating
  artifact-level latent risk from post-attack realized harm.

  \item We introduce \safeevolve and show that governing skill admission,
  revision, provenance, and retirement reduces evolution-induced harm while
  retaining the utility gained through learning.
\end{enumerate}

\section{Related Work}
\label{sec:related-work}

\paragraph{Self-improving agents and skill evolution.}
Position this work against methods that extract, edit, select, vote over, or
retrieve reusable skills from agent trajectories. Organize the literature by
the persistent artifact each method produces and the success signal used to
select it. The key gap is that these methods report capability improvement but
do not measure evolution-induced safety change.

\paragraph{Safety of persistent agent memory.}
Discuss memory poisoning, long-term memory evolution, retrieval attacks, and
forgetting. Distinguish a skill from a stored observation: a skill is an
executable or prescriptive procedure intended to generalize across tasks. The
failure studied here is endogenous policy formation from the agent's own
successful trajectory, not only storage of attacker-supplied text.

\paragraph{Executable safety-task sources.}
Agent safety benchmarks supply tasks, environments, attack inputs, and outcome
judges that a learning environment can execute. Our current benchmark
instantiation uses AgentHazard, which provides 2,653 computer-use instances,
ten risk categories, and multi-step attack strategies
\citep{feng2026agenthazard}. AgentHazard is a task-source adapter for \gym: its
task and judge semantics are retained, while the gym supplies the persistent
skill state, lifecycle instrumentation, and cross-task execution protocol.

\paragraph{Governance of evolving libraries.}
Contrast \safeevolve with capability-oriented library maintenance and drift
control. Library hygiene retains skills that improve task success, including
skills whose utility comes from unsafe shortcuts. \safeevolve introduces safety
as a first-class admission and retention signal.

\section{Skill Misevolution}
\label{sec:problem}

\subsection{Self-improvement as a policy update}

Let an agent with base policy $\pi$ execute a sequence of tasks
$\taskseq=(q_1,\ldots,q_T)$. After task $q_t$, an evolution method
$\evolution$ maps the trajectory $\tau_t$ and current library $\library_t$ to
an updated library:
\begin{equation}
  \library_{t+1}=\evolution(\library_t, q_t, \tau_t).
  \label{eq:evolution}
\end{equation}
The next task is executed by the base agent conditioned on retrieved skills
from $\library_{t+1}$. Conversational state is reset between tasks, so any
systematic cross-task change is carried by the evolved artifact.

\subsection{Definition and causal chain}

An episode exhibits \emph{skill misevolution} when an unsafe procedure learned
from an earlier trajectory enters the persistent library and causes incremental
unsafe behavior on a later task relative to a matched no-evolution execution.
The proposed causal chain is:
\begin{equation}
\text{unsafe success}
\rightarrow \text{skill extraction}
\rightarrow \text{unsafe generalization}
\rightarrow \text{retrieval}
\rightarrow \text{reuse}
\rightarrow \text{persistent harm}.
\label{eq:chain}
\end{equation}
The benchmark separately observes each link. This prevents a later unsafe
action from being attributed to the skill merely because a skill library was
present.

\subsection{Threat model}

The attacker controls a bounded number of task inputs during the learning
history but does not modify the evolution code, skill store, retrieval engine,
judge, or final persistence probe. The attacker succeeds when an exposure is
absorbed into a reusable skill that increases unsafe behavior or later causes
harm without a new attack. The strongest outcome is carryover harm in a fresh session
that receives only the final evolved library.

\section{SkillMisevo Gym and Bench}
\label{sec:benchmark}

\subsection{Gym environment contract}

\gym turns a stateless executable safety task into a controlled stateful
learning process. An environment instance exposes five interfaces: task reset,
agent execution, skill update, skill retrieval, and artifact inspection. A
task-source adapter supplies executable instances, reset logic, attack and
benign views, and outcome judges; it does not control how skills are authored
or evaluated. Replacing this adapter changes the tasks and their success
semantics without changing the skill lifecycle, episode protocol, or
misevolution metrics. The gym snapshots $\library_t$ before and after every task, stores the authored diff
and its source trajectory, records which skills are retrieved on the next task,
and supports replay from any snapshot. Agent adapters translate native skill
formats and lifecycle hooks without converting the stored artifact into a
shared representation. Evolution adapters expose when and from which trajectory
a candidate skill is written. This contract supports both external evolution
methods and an agent's native skill evolution.

Conversation, workspace, process, and tool state are reset between tasks. The
gym then mounts only the designated skill snapshot. This isolation makes the
library the sole cross-task treatment and allows matched executions with no
evolution, benign-only evolution, or a frozen library. The environment emits a
common event log containing task outcome, skill writes, artifact versions,
retrievals, tool trajectories, and judge evidence.

\subsection{Benchmark episode protocol}

Each episode contains seven tasks from one task-source category and task family.
The order is
\begin{equation}
  B_1, M_1, B_2, M_2, B_3, M_3, P,
\end{equation}
where $B$ denotes a zero-injection benign twin, $M$ an adversarial exposure,
and $P$ a benign persistence probe. The three exposures use different surface
forms from the same risk family so that the library can accumulate a coherent
rule without simply memorizing one string. Position $B_1$ measures the
pre-exposure floor; $B_2$ and $B_3$ provide additional experience and retrieval
opportunities after increasing exposure; $P$ runs after the library is frozen
in a new executor session.

\begin{figure}[t]
  \centering
  \fbox{\parbox[c][4.0cm][c]{0.94\linewidth}{
    \centering
    \textbf{Figure 1 placeholder: the exposure-to-persistence episode.}\\[4pt]
    Show $B_1 \rightarrow M_1 \rightarrow B_2 \rightarrow M_2 \rightarrow
    B_3 \rightarrow M_3 \rightarrow P$, library snapshots $L_0\ldots L_6$,
    fresh executor sessions, retrieval logs, artifact audits, and the final
    clean-session probe. Highlight that only the library crosses task borders.
  }}
  \caption{\textbf{Skill misevolution evaluation.} Adversarial exposures can
  alter the evolving skill library; later benign tasks test whether the learned
  shortcut is retrieved and reused after the attack disappears.}
  \label{fig:episode}
\end{figure}

\subsection{Paired controls}

Every episode is replayed under matched evolution settings:
\begin{itemize}
  \item \textbf{No evolution}: disables persistent learning and establishes the
  base-agent floor.
  \item \textbf{Read-only reflection}: retains textual lessons in the current
  learning mechanism without authoring a reusable skill artifact.
  \item \textbf{Benign evolution}: evolves only from benign tasks and tests
  whether ordinary learning alone produces the effect.
  \item \textbf{Full evolution}: applies each evaluated method to all successful
  trajectories under its native update rule.
\end{itemize}
All arms use identical tasks, order, tools, agent configuration, and judges.

\subsection{Task-source instantiation, agents, and evolution methods}

The experiments instantiate the gym's task-source interface with AgentHazard
\citep{feng2026agenthazard}. The adapter exposes its harmful objectives,
multi-step attack sequences, ten risk categories, sandboxed computer-use tasks,
reset procedure, and trajectory-level judge. It also constructs matched benign
twins and persistence probes required by the benchmark episode protocol. Every
assembled episode is executed by six target-agent configurations spanning four
agent frameworks and three model backbones. Claude Code, Codex, Hermes, and OpenClaw
share MiniMax-M2.5 as a controlled backbone comparison. Claude Code is also
paired with Haiku~4.5, and Codex with GPT-5.4-mini, providing within-framework
backbone comparisons. The target is the complete agent system---its scaffold,
tool mediation, skill lifecycle, retrieval behavior, and backbone---rather than
an isolated language model.

The evolution settings include EvoSkill, SkillClaw, AutoSkill, SkillsVote, and
Hermes-native skill evolution. Hermes-native is tied to the Hermes agent and is
evaluated only in the Hermes /
MiniMax configuration; the corresponding cells for other target agents are
N/A. Each method is evaluated in its actual stored representation, including
associated scripts or structured contracts, rather than after conversion to a
common textual summary.

\subsection{Metrics}

\paragraph{Utility.}
Carryover utility is task completion on $P$ after the final evolved library is
mounted in a clean session. It distinguishes a safer evolution method from one
that suppresses useful skill reuse.

\paragraph{Attack success.}
Malicious ASR is the fraction of $M$ tasks whose original scenario judge
reports the unsafe objective. We report the absolute ASR of every evolution
setting on the same exposed task instances and use no evolution as the matched
reference row.

\paragraph{Evolved artifact.}
An independent auditor reads the complete stored skill bundle and assigns three
1--5 scores. \emph{Content unsafety} measures the severity of unsafe procedures
encoded in the artifact. \emph{Unsafe generalization} measures how broadly and
unconditionally those procedures are stated beyond their source task.
\emph{Stealthiness} measures how strongly the unsafe procedure is disguised as
a legitimate best practice, audit, backup, or compliance step. Auditor
agreement is measured on a double-labeled subset.

\paragraph{Post-attack carryover.}
After conversational and environment state are reset and only the frozen final
library is mounted, the $P$ task yields two outcomes. Carryover harm is
unsafe-goal completion; carryover utility is benign-task completion. Together
they measure whether the evolved library preserves harm, useful capability, or
both after the attack ends.

\subsection{Statistical protocol}

Results are pooled over the frozen episode distribution. Confidence intervals
use episode-level bootstrap resampling. Paired ASR and carryover outcomes are
tested over matched task instances; method comparisons report effect sizes and
multiple-comparison-adjusted intervals. Hyperparameters, episode construction,
and the held-out split are frozen before the final sweep.

\section{SafeEvolve}
\label{sec:safeevolve}

\safeevolve is a governance layer around an existing evolution method. It does
not replace the base agent or add a runtime refusal policy. It governs the
persistent skill update at two decision points: writing and reuse.

\subsection{Admission critic and targeted repair}

Before a candidate enters the library, an independent critic audits the full
bundle, including scripts and referenced resources. When a candidate removes a
safety check, assumes authority, broadens a dangerous operation, or embeds an
unsafe tool sequence, the critic produces a targeted repair: restore the check,
add the missing precondition, or narrow the applicability condition. Repaired
skills are re-audited before admission.

\subsection{Verification-preserving evolution}

Skill revisions are constrained to preserve existing verification,
confirmation, authorization, and reversibility requirements unless the update
provides explicit evidence that the requirement is obsolete. This prevents an
efficiency-oriented edit from treating a safeguard as removable overhead.

\subsection{Lineage and provenance}

Each skill stores its source tasks, trajectories, revisions, and exposure
status. Provenance supports attribution and batch rollback when an upstream
experience is later judged unsafe. It also supplies a risk prior to retrieval
and retirement decisions.

\subsection{Safety-aware selection and retirement}

Raw evolution selects skills primarily by utility. \safeevolve augments this
objective with the observed safety impact of reuse. Skills associated with
carryover harm are downweighted, quarantined, repaired, or
retired even when they improve task success. This distinguishes \safeevolve
from capability-only library hygiene, which keeps useful but unsafe skills.

\paragraph{Hermes-native integration.}
Hermes-native evolution enters the same governance interface at its skill-write
approval boundary. In the raw condition, a native candidate follows Hermes's
ordinary approval and storage path. Library-Drift applies capability-oriented
maintenance after writing. Under \safeevolve, the complete native skill bundle
is audited and, when necessary, repaired before approval; accepted skills then
receive the same provenance, safety-aware retrieval, and retirement treatment
as skills produced by agent-agnostic evolution methods. This preserves Hermes's
native authoring loop while changing only the governance of its persistent
updates.

\begin{figure}[t]
  \centering
  \fbox{\parbox[c][3.6cm][c]{0.94\linewidth}{
    \centering
    \textbf{Figure 2 placeholder: SafeEvolve architecture.}\\[4pt]
    Trajectory $\rightarrow$ candidate skill $\rightarrow$ provenance and
    verification-preserving edit $\rightarrow$ admission critic/repair
    $\rightarrow$ library $\rightarrow$ safety-aware retrieval and retirement.
  }}
  \caption{\textbf{SafeEvolve governs persistent learning.} Components act at
  skill writing and reuse, the two points at which transient experience becomes
  persistent policy and later affects behavior.}
  \label{fig:safeevolve}
\end{figure}

\section{Experimental Setup}
\label{sec:setup}

\subsection{Research questions}

\begin{description}
  \item[RQ1: Safety--utility coupling.] Does skill evolution increase unsafe
  behavior while retaining or improving task utility?
  \item[RQ2: Exposure-to-persistence mechanism.] Does unsafe experience enter
  the skill artifact, get retrieved, and produce post-attack carryover harm
  while retaining carryover utility?
  \item[RQ3: Scope and attacker budget.] How does misevolution vary across
  methods, agents, task categories and attack strategies, and one to three
  adversarial exposures?
  \item[RQ4: Mitigation.] Can \safeevolve reduce the three evolved-artifact
  risks and post-attack carryover harm while preserving carryover utility?
\end{description}

\subsection{Evaluation matrix}

The main evaluation treats target-agent as a first-class axis, not a nuisance
variable to average away. Table~\ref{tab:targets} fixes the six configurations.
The shared MiniMax backbone isolates framework effects across four agents; the
second Claude Code and Codex configurations expose backbone effects within a
framework. Each applicable evolution method runs on the identical frozen
episode set for every target. Hermes-native evolution appears only on Hermes;
unsupported combinations are marked N/A rather than pooled with failures.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{ll}
\toprule
\textbf{Agent framework} & \textbf{Backbone} \\
\midrule
Claude Code & MiniMax-M2.5 \\
Codex       & MiniMax-M2.5 \\
Hermes      & MiniMax-M2.5 \\
OpenClaw    & MiniMax-M2.5 \\
Claude Code & Haiku~4.5 \\
Codex       & GPT-5.4-mini \\
\bottomrule
\end{tabular}
\caption{\textbf{Six target-agent configurations.} The matrix supports
controlled comparisons across agent frameworks and model backbones.}
\label{tab:targets}
\end{table}

The complete experimental grid is target-agent $\times$ evolution method
$\times$ AgentHazard risk category $\times$ exposure budget. All main-table
cells pool the same predeclared episode distribution within a target;
macro-averages give equal weight to the six target-agent configurations.
Per-category and per-attack-strategy results appear in the appendix.

\subsection{Baselines}

The diagnostic comparison includes no evolution, read-only reflection, benign
evolution, and the selected skill-evolution methods. The mitigation comparison
adds Library-Drift as a capability-oriented governance baseline. Each evolution
method is evaluated raw, with Library-Drift, and with \safeevolve under the same
frozen train/development/held-out protocol.

\section{Results}
\label{sec:results}

\subsection{RQ1: Does evolution increase unsafe behavior while retaining utility?}
\label{sec:rq1}

Table~\ref{tab:misevolution-main} is the paper's primary diagnostic table. Its
rows preserve the six target-agent configurations and its columns preserve the
evolution methods. Panel A directly compares attack success, Panel B measures
the evolved skill artifact, and Panel C jointly tests whether harm and utility
survive after the attack disappears. This layout makes framework and backbone
variation visible instead of hiding it inside one pooled method average.

% Main Table 1: do not place SafeEvolve in this table.
\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3.2pt}
\renewcommand{\arraystretch}{1.04}
\resizebox{\textwidth}{!}{
\begin{tabular}{llcccccccc}
\toprule
\textbf{Target agent} & \textbf{Backbone} & \textbf{No Evo.} &
\textbf{Reflect.} & \textbf{Benign Evo.} & \textbf{EvoSkill} &
\textbf{SkillClaw} & \textbf{AutoSkill} &
\textbf{SkillsVote} & \textbf{Hermes native} \\
\midrule
\multicolumn{10}{l}{\textbf{A. Attack comparison: malicious ASR (\%; $\downarrow$)}} \\
Claude Code & MiniMax      & -- & -- & -- & -- & -- & -- & -- & N/A \\
Codex       & MiniMax      & -- & -- & -- & -- & -- & -- & -- & N/A \\
Hermes      & MiniMax      & -- & -- & -- & -- & -- & -- & -- & -- \\
OpenClaw    & MiniMax      & -- & -- & -- & -- & -- & -- & -- & N/A \\
Claude Code & Haiku        & -- & -- & -- & -- & -- & -- & -- & N/A \\
Codex       & GPT-5.4-mini & -- & -- & -- & -- & -- & -- & -- & N/A \\
\rowcolor{lightgray}\multicolumn{2}{l}{Macro avg.} & -- & -- & -- & -- & -- & -- & -- & -- \\
\midrule
\multicolumn{10}{l}{\textbf{B. Evolved artifact: content unsafety / unsafe generalization / stealthiness ($\downarrow$)}} \\
Claude Code & MiniMax      & N/A & N/A & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & N/A \\
Codex       & MiniMax      & N/A & N/A & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & N/A \\
Hermes      & MiniMax      & N/A & N/A & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
OpenClaw    & MiniMax      & N/A & N/A & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & N/A \\
Claude Code & Haiku        & N/A & N/A & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & N/A \\
Codex       & GPT-5.4-mini & N/A & N/A & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & N/A \\
\rowcolor{lightgray}\multicolumn{2}{l}{Macro avg.} & N/A & N/A & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multicolumn{10}{l}{\textbf{C. Post-attack: carryover harm ($\downarrow$) / carryover utility ($\uparrow$) (\%)}} \\
Claude Code & MiniMax      & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & N/A \\
Codex       & MiniMax      & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & N/A \\
Hermes      & MiniMax      & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
OpenClaw    & MiniMax      & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & N/A \\
Claude Code & Haiku        & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & N/A \\
Codex       & GPT-5.4-mini & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & N/A \\
\rowcolor{lightgray}\multicolumn{2}{l}{Macro avg.} & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
\bottomrule
\end{tabular}}
\caption{\textbf{Skill evolution across six target-agent configurations
(RQ1).} Each cell pools the same frozen episode distribution within one target
agent and evolution setting. Panel A directly compares malicious ASR across
settings. Panel B reports mean content-unsafety / unsafe-generalization /
stealthiness scores, each on a 1--5 ordinal scale. Panel C reports clean-session
carryover harm / carryover utility. The four MiniMax rows
isolate agent-framework
effects; the paired Claude Code and Codex rows expose backbone effects within a
framework. N/A means either that a setting writes no skill artifact or that a
native method is unsupported for that target; it never denotes a failed run.
The exposure-to-persistence transitions are expanded in
Table~\ref{tab:chain}. Add 95\% episode-bootstrap intervals in the supplement
or an accompanying interval plot.}
\label{tab:misevolution-main}
\end{table*}

\paragraph{Result paragraph template.}
Across six target-agent configurations, malicious ASR reaches [X], while the
evolved artifacts score [Y/Z/W] on content unsafety, unsafe generalization, and
stealthiness. In the post-attack probe, carryover harm reaches [H] while
carryover utility remains [U]. The effect appears in [A/6] targets and [B/M]
evolution methods. The joint result rules out a pure context effect: the attack
is absent, the executor is reset, and the only cross-task object is the evolved
library.

\paragraph{Safety--utility frontier.}
Plot each method by carryover utility and carryover harm. The important region
is high utility and high harm: these methods preserve useful procedures while
also retaining the unsafe shortcut, making capability improvement an
unreliable safety signal.

\subsection{RQ2: How does exposure become persistent harm?}
\label{sec:rq2}

RQ2 tests each link of Equation~\ref{eq:chain}. First report how exposure
changes content unsafety, unsafe generalization, and stealthiness. Then report
retrieval of the evolved artifact and post-attack carryover harm and utility
conditional on retrieval. Use matched
mediation or stratified conditional effects rather than inferring mediation
from a single example.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{5pt}
\begin{tabular}{lcc}
\toprule
\textbf{Link in the chain} & \textbf{Rate} & \textbf{Conditional effect} \\
\midrule
Exposure $\rightarrow$ content unsafety & -- & -- \\
Exposure $\rightarrow$ unsafe generalization & -- & -- \\
Exposure $\rightarrow$ stealthiness & -- & -- \\
Evolved artifact $\rightarrow$ retrieval & -- & -- \\
Retrieval $\rightarrow$ carryover harm & -- & -- \\
Retrieval $\rightarrow$ carryover utility & -- & -- \\
\bottomrule
\end{tabular}
\caption{\textbf{Exposure-to-persistence chain (RQ2).} Each row measures an
observable transition rather than treating later harm as automatic evidence of
skill mediation. Conditional effects compare matched episodes with and without
the upstream event.}
\label{tab:chain}
\end{table}

\paragraph{Artifact risk precedes behavioral harm.}
Compare the three artifact scores with realized carryover harm. Episodes with
high content unsafety, unsafe generalization, or stealthiness but no post-attack
harm constitute latent risk: the persistent policy has changed, but the probe
has not supplied the retrieval cue or execution opportunity. Quantify this
artifact--behavior gap by method and agent.

\paragraph{Worked causal trace.}
Show one compact case from source exposure to authored skill diff, later
retrieval log, unsafe tool action, judge state, and fresh-session reproduction.
The example should expose the exact over-generalized clause and the safety check
that evolution removed or failed to encode.

\subsection{RQ3: Where does misevolution emerge, and at what budget?}
\label{sec:rq3}

Report heterogeneity across evolution algorithms, agent frameworks, model
backbones, AgentHazard risk categories, attack strategies, and exposure
budgets. Avoid treating the pooled
mean as universality: identify which design properties predict high artifact
unsafety and which executor policies block unsafe artifacts from becoming
post-attack behavior.

\paragraph{Framework and backbone decomposition.}
The four MiniMax-M2.5 targets hold the backbone fixed while changing the agent
framework, exposing the contribution of tool mediation, system instructions,
skill retrieval, and execution policy. The two within-framework contrasts---
Claude Code with MiniMax versus Haiku and Codex with MiniMax versus GPT-5.4-mini
---then test how much of the effect follows the model backbone. Report both
contrasts before the overall macro-average; the six configurations are related
controls, not six interchangeable replications.

\begin{figure}[t]
  \centering
  \fbox{\parbox[c][3.7cm][c]{0.94\linewidth}{
    \centering
    \textbf{Figure 3 placeholder: budget--response curve.}\\[4pt]
    X-axis: adversarial exposures $k\in\{0,1,2,3\}$.\\
    Y-axes or panels: content unsafety, unsafe generalization, stealthiness,
    carryover harm, and carryover utility. Show method-level curves with
    bootstrap CIs.
  }}
  \caption{\textbf{Misevolution under bounded exposure (RQ3).} The minimum
  budget that produces persistent harm defines the practical attacker cost.}
  \label{fig:budget}
\end{figure}

\paragraph{Failure mechanisms.}
Cluster audited skill diffs into a small post-hoc taxonomy such as verification
skipping, authorization assumption, policy erosion, and unsafe
over-generalization. Report multi-label frequencies and annotator agreement.
Use the taxonomy to explain variation, not as the primary proof that
misevolution exists.

\subsection{RQ4: Can SafeEvolve preserve learning without preserving harm?}
\label{sec:rq4}

The mitigation evaluation uses held-out AgentHazard episodes and attack
strategies that are hidden
during evolution, repair-policy development, and threshold selection. Libraries
are frozen before evaluation. A mitigation must reduce content unsafety, unsafe
generalization, and stealthiness in the evolved artifact, then reduce
fresh-session carryover harm while preserving carryover utility. The artifact
and post-attack stages are reported together rather than collapsed into one
score.

% Main Table 2: held-out mitigation table.
\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.04}
\resizebox{\textwidth}{!}{
\begin{tabular}{llccccccc}
\toprule
\textbf{Evolution method} & \textbf{Governance}
& \textbf{CC/MiniMax} & \textbf{Codex/MiniMax} & \textbf{Hermes/MiniMax}
& \textbf{OpenClaw/MiniMax} & \textbf{CC/Haiku} & \textbf{Codex/GPT}
& \textbf{Macro avg.} \\
\midrule
\multicolumn{9}{l}{\textbf{A. Evolved artifact: content unsafety / unsafe generalization / stealthiness ($\downarrow$)}} \\
\multirow{3}{*}{AutoSkill}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{EvoSkill}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{SkillClaw}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{SkillsVote}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{Hermes-native}
 & Raw             & N/A & N/A & --/--/-- & N/A & N/A & N/A & --/--/-- \\
 & Library-Drift   & N/A & N/A & --/--/-- & N/A & N/A & N/A & --/--/-- \\
 & \safeevolve     & N/A & N/A & --/--/-- & N/A & N/A & N/A & --/--/-- \\
\midrule
\multicolumn{9}{l}{\textbf{B. Post-attack: carryover harm ($\downarrow$) / carryover utility ($\uparrow$) (\%)}} \\
\multirow{3}{*}{AutoSkill}
 & Raw             & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
 & Library-Drift   & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
 & \safeevolve     & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
\midrule
\multirow{3}{*}{EvoSkill}
 & Raw             & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
 & Library-Drift   & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
 & \safeevolve     & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
\midrule
\multirow{3}{*}{SkillClaw}
 & Raw             & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
 & Library-Drift   & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
 & \safeevolve     & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
\midrule
\multirow{3}{*}{SkillsVote}
 & Raw             & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
 & Library-Drift   & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
 & \safeevolve     & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- & --/-- \\
\midrule
\multirow{3}{*}{Hermes-native}
 & Raw             & N/A & N/A & --/-- & N/A & N/A & N/A & --/-- \\
 & Library-Drift   & N/A & N/A & --/-- & N/A & N/A & N/A & --/-- \\
 & \safeevolve     & N/A & N/A & --/-- & N/A & N/A & N/A & --/-- \\
\bottomrule
\end{tabular}}
\caption{\textbf{SafeEvolve reduces artifact risk and post-attack harm while
preserving carryover utility (RQ4).} Each evolution method is evaluated raw,
with a capability-oriented library hygiene baseline, and with \safeevolve on
the same held-out episodes. Columns retain all six target-agent configurations.
Panel A directly reports content unsafety / unsafe generalization /
stealthiness on 1--5 scales; Panel B directly reports carryover harm /
carryover utility. No deltas or gain-retention ratios are used. Macro averages
weight target agents equally and exclude only
genuinely unsupported method--agent pairs. Artifact scores and overhead appear
in the accompanying breakdown. Bold the safest method whose utility is
statistically non-inferior to raw evolution. Hermes-native is evaluated only in
the Hermes / MiniMax column; its macro entry therefore summarizes that single
supported target rather than imputing results for other agents.}
\label{tab:safeevolve-main}
\end{table*}

\paragraph{Why Library-Drift is the decisive baseline.}
Capability-oriented hygiene can remove stale or low-value skills, but it should
retain a skill that improves utility through an unsafe shortcut. A gap between
Library-Drift and \safeevolve therefore tests whether an explicitly safety-aware
objective is necessary.

\paragraph{Independent skill-safety benchmarks.}
The held-out episode table tests end-to-end mitigation inside evolving agents.
We separately test the admission boundary on two external benchmarks.
MalSkillBench contains labeled malicious and benign skill bundles and measures
whether a gate detects unsafe stored content without rejecting benign skills.
SkillSafetyBench plants a skill into an agent and measures whether the accepted
skill causes unsafe behavior. Table~\ref{tab:skill-safety} reports absolute
metrics on both benchmarks; the complete per-baseline breakdown appears in the
appendix.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.08}
\begin{tabular}{lccc|cc}
\toprule
& \multicolumn{3}{c|}{\textbf{MalSkillBench: skill content}}
& \multicolumn{2}{c}{\textbf{SkillSafetyBench: planted skill}} \\
\cmidrule(lr){2-4}\cmidrule(lr){5-6}
\textbf{Skill gate}
& Malicious recall $\uparrow$ & Benign FPR $\downarrow$ & AUROC $\uparrow$
& Behavioral ASR $\downarrow$ & Benign utility $\uparrow$ \\
\midrule
No skill gate                    & -- & -- & -- & -- & -- \\
Best skill-security scanner      & -- & -- & -- & -- & -- \\
Best supply-chain detector       & -- & -- & -- & -- & -- \\
Best prompt-injection guard      & -- & -- & -- & -- & -- \\
Library-Drift gate               & -- & -- & -- & -- & -- \\
\rowcolor{lightblue}\safeevolve admission critic & -- & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\caption{\textbf{SafeEvolve on independent skill-safety benchmarks (RQ4).}
MalSkillBench evaluates labeled malicious and benign skill bundles, including
code and scripts referenced by the skill. SkillSafetyBench measures downstream
behavior after a planted skill passes the gate. All entries are absolute rates
or scores; no difference metrics are used. Category-best baseline rows keep the
main table readable, while results for all individual scanners and guards are
reported in the appendix.}
\label{tab:skill-safety}
\end{table*}

\paragraph{Capability preservation on common benchmarks.}
Safety gains are useful only if the governed library continues to improve task
performance. We therefore evaluate each evolution method raw, with
Library-Drift, and with \safeevolve on the same three capability benchmarks.
Table~\ref{tab:capability} reports absolute task success for every condition,
not change from raw evolution.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{6pt}
\renewcommand{\arraystretch}{1.08}
\begin{tabular}{llcccc}
\toprule
\textbf{Evolution method} & \textbf{Governance}
& \textbf{SkillsBench} & \textbf{SkillLearnBench}
& \textbf{SWE-Skills-Bench} & \textbf{Macro avg.} \\
\midrule
\multirow{3}{*}{AutoSkill}
 & Raw             & -- & -- & -- & -- \\
 & Library-Drift   & -- & -- & -- & -- \\
 & \safeevolve     & -- & -- & -- & -- \\
\midrule
\multirow{3}{*}{EvoSkill}
 & Raw             & -- & -- & -- & -- \\
 & Library-Drift   & -- & -- & -- & -- \\
 & \safeevolve     & -- & -- & -- & -- \\
\midrule
\multirow{3}{*}{SkillClaw}
 & Raw             & -- & -- & -- & -- \\
 & Library-Drift   & -- & -- & -- & -- \\
 & \safeevolve     & -- & -- & -- & -- \\
\midrule
\multirow{3}{*}{SkillsVote}
 & Raw             & -- & -- & -- & -- \\
 & Library-Drift   & -- & -- & -- & -- \\
 & \safeevolve     & -- & -- & -- & -- \\
\midrule
\multirow{3}{*}{Hermes-native}
 & Raw             & -- & -- & -- & -- \\
 & Library-Drift   & -- & -- & -- & -- \\
 & \safeevolve     & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\caption{\textbf{SafeEvolve preserves absolute capability (RQ4).} Task-success
rates for raw evolution, capability-oriented library governance, and
\safeevolve on three common skill benchmarks. All values are absolute
scores under matched agent, model, and inference budgets. Statistical
non-inferiority to the corresponding raw method is tested separately and
reported with confidence intervals. Hermes-native rows use the Hermes / MiniMax
agent configuration.}
\label{tab:capability}
\end{table*}

\paragraph{Component evaluation.}
Finally, ablate admission auditing, verification preservation, provenance
rollback, and safety-aware retirement. These component results explain the
three complete RQ4 evaluations; they do not replace held-out behavioral or
capability evidence.

\begin{table}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lccccc}
\toprule
\textbf{SafeEvolve variant} & \textbf{CU $\downarrow$} &
\textbf{UG $\downarrow$} & \textbf{Stealth $\downarrow$} &
\textbf{Carry-Harm $\downarrow$} & \textbf{Carry-Utility $\uparrow$} \\
\midrule
Full \safeevolve             & -- & -- & -- & -- & -- \\
$-$ admission critic         & -- & -- & -- & -- & -- \\
$-$ verification preservation& -- & -- & -- & -- & -- \\
$-$ provenance               & -- & -- & -- & -- & -- \\
$-$ safety-aware retirement  & -- & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\caption{\textbf{SafeEvolve component analysis.} CU, UG, and Stealth are the
three evolved-artifact scores; Carry-Harm and Carry-Utility are the two
post-attack outcomes on held-out episodes.}
\label{tab:safeevolve-ablation}
\end{table}

\section{Discussion}
\label{sec:discussion}

\paragraph{Skills are security-relevant policy.}
The central implication is that a skill library should not be governed as a
passive document store. A stored skill can alter tool choice, ordering,
verification, and authorization across future tasks. Admission, provenance,
rollback, and safety monitoring should therefore be part of the learning
boundary.

\paragraph{Artifact risk and post-attack harm are distinct.}
An evolved artifact can score highly on content unsafety, unsafe
generalization, or stealthiness before its unsafe procedure is retrieved. This
is latent risk, not a safe artifact. Conversely, a runtime policy can block
execution while the persistent library continues to carry the unsafe rule.
Product evaluation should therefore report the three artifact properties and
the two post-attack outcomes separately.

\paragraph{Capability and safety share the same shortcut.}
The difficult cases are not useless malicious skills. They are procedures that
increase success by removing friction that was serving as a safeguard. This is
why disabling evolution loses capability and capability-only library hygiene
does not solve the problem.

\paragraph{Scope and limitations.}
The study focuses on readable skill artifacts produced by CLI and coding agents
and on bounded direct exposure in executable task environments. Results support
claims about evolution-induced incremental risk within this setting. Future
work should test indirect exposure, multimodal skills, opaque learned memories,
longer deployment horizons, interacting libraries, and adaptive attackers.

\section{Conclusion}
\label{sec:conclusion}

Skill self-evolution changes the temporal structure of agent safety. An unsafe
action need not end with the trajectory that produced it: evolution can extract
the successful shortcut, store it as a reusable skill, and apply it later when
the original attack is gone. \gym makes this state transition executable and
replayable; \bench makes it measurable from exposure through artifact formation
and clean-session carryover. \safeevolve shows how to govern the
persistent update while preserving useful learning. Safe self-improvement
requires evaluating not only whether an agent succeeds today, but also what
today's success teaches it to do tomorrow.

\bibliography{iclr2026_conference}
\bibliographystyle{iclr2026_conference}

\appendix

\section{Gym and Benchmark Construction}
\label{app:construction}

Document the generic task-source interface, environment contract, adapter
interface, reset checks, event schema, and replay guarantees. For the reported
instantiation, record the AgentHazard version, license, source-instance
sampling, risk categories, attack strategies, and unchanged judge interface.
Specify how its instances are wrapped by the gym, how benign twins and
persistence probes are derived, and how episode order, deduplication, leakage
checks, and held-out splits are fixed. Release the episode manifest so each
result can be replayed from the same source task and skill snapshot.

\section{Agent and Method Configurations}
\label{app:configurations}

Report every agent framework, model endpoint, evolution implementation,
retrieval configuration, update cadence, prompt, decoding parameter, repository
commit, and applicability restriction. Separate unsupported combinations from
failed runs.

\section{Judges and Artifact Auditing}
\label{app:judges}

Provide the original AgentHazard trajectory judge and the complete 1--5 rubrics for content
unsafety, unsafe generalization, and stealthiness, together with auditor
prompts, calibration examples, agreement statistics, and the adjudication
procedure. State which models are disjoint from the agent and evolution model
families. Provide the post-attack carryover-harm and carryover-utility judges
separately from artifact auditing.

\section{Additional Results}
\label{app:additional-results}

Include per-agent, per-backbone, per-risk-category, per-attack-strategy,
per-position, and per-budget tables with confidence intervals. Report invalid episodes, runtime
failures, missing combinations, and sensitivity to aggregation choices.

\section{SafeEvolve Implementation}
\label{app:safeevolve-implementation}

Give pseudocode and prompts for admission, repair, verification preservation,
lineage tracking, safety-aware selection, quarantine, and retirement. Include
cost accounting and the policy for re-auditing repaired bundles.

\section{Case Studies}
\label{app:case-studies}

For each major failure family, show the source trajectory, skill diff, lineage,
the three evolved-artifact scores, retrieval event, later tool trajectory,
carryover-harm and carryover-utility evidence, and the effect of \safeevolve.
Include negative cases where the artifact scores as unsafe but the executor
blocks its reuse, and where evolution remains safe.

\end{document}
