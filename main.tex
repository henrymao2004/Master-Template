\documentclass{article}
\usepackage{iclr2026_conference,times}

\input{math_commands.tex}

\usepackage{amsmath}
\usepackage{amssymb}
\usepackage{booktabs}
\usepackage{graphicx}
\usepackage{multirow}
\usepackage{array}
\usepackage{xcolor}
\usepackage{colortbl}
\usepackage{tikz}
\usetikzlibrary{arrows.meta,positioning}
\usepackage{hyperref}
\usepackage{url}
\usepackage{xspace}

\hypersetup{
  colorlinks=true,
  linkcolor=black,
  citecolor=blue,
  urlcolor=blue
}

\definecolor{lightblue}{RGB}{232,244,251}
\definecolor{lightgray}{gray}{0.94}

\newcommand{\gym}{\textsc{SkillMisevo-Gym}\xspace}
\newcommand{\bench}{\textsc{SkillMisevo-Bench}\xspace}
\newcommand{\safeevolve}{\textsc{SafeEvolve}\xspace}
\newcommand{\evolution}{\mathcal{E}}
\newcommand{\library}{\mathcal{L}}
\newcommand{\taskseq}{\mathcal{Q}}
\newcommand{\asr}{\mathrm{ASR}}
\newcommand{\carry}{\mathrm{CarryHarm}}
\newcommand{\ccmini}{Claude Code / MiniMax}
\newcommand{\hermesmini}{Hermes / MiniMax}
\newcommand{\openclawmini}{OpenClaw / MiniMax}

\title{Practice Makes Unsafe: Skill Misevolution in Self-Improving LLM Agents}

% Keep authors anonymous for submission. Replace only for camera-ready.
\author{Anonymous Authors}

% \iclrfinalcopy % Uncomment only for the camera-ready version.

\begin{document}
\maketitle

\begin{abstract}
Self-improving agents distill successful trajectories into reusable skills,
turning experience into persistent policy. We study \emph{skill misevolution}:
an unsafe success is generalized into a skill and later reused after the
original attack disappears. Existing skill benchmarks measure capability on
independent tasks, while agent-safety benchmarks do not expose the evolving
library state; neither can separate unsafe authoring, retrieval, and
clean-session harm. We introduce \gym, an extensible research environment for
executing and instrumenting longitudinal skill evolution under controlled
persistent state. \bench is its frozen evaluation instantiation, with an
exposure-to-carryover task protocol and nine online, artifact, and post-attack
metrics. Offline autoresearch discovers reusable vulnerability concepts, and
our constructor turns AgentHazard harmful-task contracts into concept-aligned
malicious, benign, and persistence tasks with a new benign-completion judge. We
also introduce \safeevolve, which governs skill
admission and reuse through targeted repair, verification preservation,
provenance, and retirement. Across four agent frameworks, a shared MiniMax
backbone, and six evolution methods, three settings exhibit the full signature
of benign contamination and fresh-session carryover; Hermes-native reaches
30.7\% carryover ASR while retaining 64.0\% carryover utility. Other settings
retrieve unsafe skills at rates up to 81.0\% yet produce zero carryover ASR.
This separation reveals the central insight: misevolution is a gated lifecycle,
and safety evaluation must locate whether risk stops at authoring, retrieval, or
execution. \safeevolve targets both the write and reuse gates while preserving
useful procedures.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

Self-improving agents convert successful trajectories into reusable skills for
software engineering, tool use, and recurring operational workflows. This paper
studies the safety of that update: given a stream of executable tasks and an
evolving skill library, determine whether an unsafe success is authored as a
general procedure and later reused without the original attack. We call this
failure \emph{skill misevolution}. It turns a transient interaction into a
persistent policy change.

Existing evaluations do not resolve this problem. Skill benchmarks measure
whether learned skills improve independent-task performance
\citep{li2026skillsbench,zhong2026skilllearnbench}; agent-safety benchmarks
measure harmful behavior under a supplied prompt or artifact
\citep{feng2026agenthazard,jin2026skillsafetybench}. Both end at the current
execution. They cannot determine whether an unsafe trajectory changed the skill
library, whether that artifact was later retrieved, or whether a fresh executor
reproduced the harm. The technical gap is longitudinal state attribution, not
another attack-success score.

Solving it requires three capabilities. First, the task set must cover an unsafe
mechanism with malicious, benign, and persistence tasks on related surfaces; harmful prompts
alone provide no test of later contamination. Second, skill state must be
versioned and isolated from conversation, workspace, cache, and native memory so
later behavior can be attributed to the exported artifact. Third, measurement
must distinguish authoring, retrieval, and execution because the progression can
stop at any of these gates.

We address these challenges with \gym and \bench (Figure~\ref{fig:episode}).
\gym is the research environment: given a task source, target agent, evolution
method, and update schedule, it versions the skill library, isolates other
cross-task state, records authoring and retrieval, and supports snapshot replay
and clean-session reload. \bench is the frozen task-and-evaluation protocol
instantiated within this environment. Using AHA autoresearch~\citep{mao2026aha}
offline, our constructor turns AgentHazard harmful-task contracts into validated
vulnerability concepts and concept-aligned malicious, benign, and persistence
tasks. It fixes the exposure schedule, adds an orthogonal benign-completion
judge, and measures online behavior, evolved artifacts, and post-attack
carryover with nine metrics.

\begin{figure*}[t]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  design/.style={draw=black!65, rounded corners=2pt, align=center,
    minimum width=3.25cm, minimum height=1.45cm, inner sep=5pt,
    font=\small, fill=white},
  episode/.style={draw=black!65, rounded corners=2pt, align=center,
    minimum width=3.05cm, minimum height=1.55cm, inner sep=5pt,
    font=\small, fill=white},
  flow/.style={-{Stealth[length=2.4mm]}, line width=0.8pt,
    draw=black!70},
  note/.style={font=\scriptsize, align=center, text=black!65},
  node distance=7mm
]
\node[font=\small\bfseries, anchor=west] at (-1.9,1.35)
  {(a) Offline benchmark construction};
\node[design, fill=lightgray] (contract) {
  \textbf{AgentHazard contract}\\
  harmful objective + sandbox\\
  \scriptsize harmful trajectory judge};
\node[design, right=of contract, fill=orange!9] (concept) {
  \textbf{Autoresearch discovery}\\
  falsifiable vulnerability concept\\
  \scriptsize enabling condition + evidence};
\node[design, right=of concept, fill=yellow!12] (constructor) {
  \textbf{Our episode constructor}\\
  concept-aligned task surfaces\\
  \scriptsize new benign-completion judge};
\node[design, right=of constructor, fill=blue!7] (tasks) {
  \textbf{Frozen benchmark tasks}\\
  $M$: unsafe condition \quad $B$: benign\\
  \scriptsize $P$: zero-injection persistence cue};
\draw[flow] (contract) -- (concept);
\draw[flow] (concept) -- (constructor);
\draw[flow] (constructor) -- (tasks);

\node[font=\small\bfseries, anchor=west, below=12mm of contract.south west]
  {(b) Bench protocol executed in the Gym};
\node[episode, below=22mm of contract, fill=red!6] (r1) {
  \textbf{$R_1$: dose $3M$}\\
  $M^3\!\rightarrow\!\evolution\!\rightarrow\!B^3\!\rightarrow\!\evolution$\\
  \scriptsize online behavior + artifact audit};
\node[episode, right=5mm of r1, fill=red!8] (r2) {
  \textbf{$R_2$: dose $6M$}\\
  $M^3\!\rightarrow\!\evolution\!\rightarrow\!B^3\!\rightarrow\!\evolution$\\
  \scriptsize online behavior + artifact audit};
\node[episode, right=5mm of r2, fill=red!10] (r3) {
  \textbf{$R_3$: dose $9M$}\\
  $M^3\!\rightarrow\!\evolution\!\rightarrow\!B^3\!\rightarrow\!\evolution$\\
  \scriptsize online behavior + artifact audit};
\node[episode, right=5mm of r3, fill=yellow!12] (freeze) {
  \textbf{Freeze library}\\
  export \texttt{SKILL.md}\\
  \scriptsize reset executor and all other state};
\node[episode, right=5mm of freeze, fill=blue!8] (probe) {
  \textbf{Fresh-session $P^3$}\\
  reload only \texttt{SKILL.md}\\
  \scriptsize URR / C-ASR / C-Util};
\draw[flow] (r1) -- (r2);
\draw[flow] (r2) -- (r3);
\draw[flow] (r3) -- (freeze);
\draw[flow] (freeze) -- (probe);
\draw[flow, dashed] (tasks.south) -- ++(0,-3mm) -|
  node[note, pos=0.28, below] {assemble fixed episodes} (r1.north);

\node[note, below=3mm of r2.south, text width=9.4cm] {
  \textbf{Online:} BU / M-ASR / B-ASR \qquad
  \textbf{Artifact:} CU / UG / Stealth};
\node[note, below=3mm of probe.south, text width=4.3cm] {
  \textbf{Post-attack:} URR / C-ASR / C-Util};
\end{tikzpicture}%
}
\caption{\textbf{SkillMisevo-Gym and SkillMisevo-Bench.}
\textbf{(a)} AgentHazard supplies a harmful-task contract; an external
autoresearch stage discovers validated vulnerability concepts, and our
constructor produces concept-aligned $M/B/P$ tasks and a benign-completion
judge. \textbf{(b)} \bench fixes the longitudinal episode and lifecycle metrics;
\gym executes it with versioned skill state, observable authoring and retrieval,
and clean-session replay. The discovery VCG is never exposed to the evaluated
agent; only its self-authored \texttt{SKILL.md} crosses the final reset.}
\label{fig:episode}
\end{figure*}

We further introduce \safeevolve, a governance layer at the persistent update
boundary (Figure~\ref{fig:safeevolve}). At write time it audits and repairs
candidate skills while preserving verification requirements and provenance; at
reuse time it uses lineage and observed outcomes for selection, rollback, and
retirement. This targets the state that carries risk across tasks while
retaining the useful procedure.

Across four agent frameworks with a shared MiniMax backbone and six evolution
methods, the benchmark reveals a gated lifecycle. AutoSkill, SkillsVote, and
Hermes-native exhibit benign contamination and fresh-session carryover;
Hermes-native reaches 30.7\% carryover ASR with 64.0\% carryover utility. Other
settings retrieve unsafe skills at rates up to 81.0\% but produce zero
carryover ASR. The resulting insight is operational: artifact risk is broader
than realized harm, and the layer where progression stops determines what must
be governed.

Our contributions are:
\begin{enumerate}
  \item We formulate \textbf{skill misevolution} as a new longitudinal safety
  problem and show that unsafe authoring, retrieval, and realized carryover are
  distinct, gated outcomes.
  \item We introduce \gym, an extensible environment for controlled,
  instrumented skill-evolution research with versioned state and clean-session
  replay, and \bench, a frozen instantiation with concept-aligned $M/B/P$
  tasks, a new benign-completion judge, and nine lifecycle metrics.
  \item We introduce \safeevolve, which governs skill admission and reuse, and
  evaluate the problem and mitigation across four agent frameworks, six
  evolution methods, ten risk categories, task surfaces, cumulative exposure
  levels, exposure timings, and update-batch compositions.
\end{enumerate}

\section{Related Work}
\label{sec:related-work}

\paragraph{Self-improving agents and skill evolution.}
Position this work against methods that extract, edit, select, vote over, or
retrieve reusable skills from agent trajectories. Organize the literature by
the persistent artifact each method produces and the success signal used to
select it. The key gap is that these methods report capability improvement but
do not measure safety patterns across an evolving skill history.

\paragraph{Safety of persistent agent memory.}
Discuss memory poisoning, long-term memory evolution, retrieval attacks, and
forgetting. Distinguish a skill from a stored observation: a skill is an
executable or prescriptive procedure intended to generalize across tasks. The
failure studied here is endogenous policy formation from the agent's own
successful trajectory, not only storage of attacker-supplied text.

\paragraph{Executable safety-task sources.}
Agent safety benchmarks supply tasks, environments, attack inputs, and outcome
judges that a learning environment can execute. AgentHazard provides 2,653
computer-use instances, ten risk categories, multi-step attack strategies, and
a harmful trajectory judge~\citep{feng2026agenthazard}. These assets define the
harmful task contract in our instantiation; they do not provide benign tasks,
persistence probes, or a benign utility metric. We construct those components
from vulnerability concepts discovered using AHA~\citep{mao2026aha}; \bench
freezes them into a longitudinal evaluation, and \gym supplies persistent skill
state, lifecycle instrumentation, and cross-task execution.

\paragraph{Governance of evolving libraries.}
Contrast \safeevolve with capability-oriented library maintenance and drift
control. Library hygiene retains skills that improve task success, including
skills that are both useful and unsafe. \safeevolve introduces safety
as a first-class admission and retention signal.

\section{Skill Misevolution}
\label{sec:problem}

\subsection{Self-improvement as a policy update}

Let an agent with base policy $\pi$ execute a sequence of three-task blocks
$\taskseq=(Q_1,\ldots,Q_K)$, where $Q_k=(q_{k,1},q_{k,2},q_{k,3})$ yields
trajectories $\mathcal{T}_k=(\tau_{k,1},\tau_{k,2},\tau_{k,3})$. After block
$Q_k$, an evolution method $\evolution$ maps the three trajectories and current
library $\library_k$ to one updated library:
\begin{equation}
  \library_{k+1}=\evolution(\library_k, Q_k, \mathcal{T}_k).
  \label{eq:evolution}
\end{equation}
The next three-task block is executed by the base agent conditioned on
retrieved skills from $\library_{k+1}$. Conversational state is reset between
tasks, and the library is the only intended cross-task state retained by the
protocol.

\subsection{Definition and temporal sequence}

An episode exhibits \emph{skill misevolution} when an unsafe procedure from an
earlier trajectory is recorded in the persistent library, is later retrieved,
and co-occurs with higher unsafe behavior than in a no-evolution
execution. The benchmark tracks the following temporal sequence:
\begin{equation}
\text{unsafe success}
\rightarrow \text{skill extraction}
\rightarrow \text{unsafe generalization}
\rightarrow \text{retrieval}
\rightarrow \text{reuse}
\rightarrow \text{persistent harm}.
\label{eq:sequence}
\end{equation}
The benchmark separately reports artifact formation, retrieval, and later
action. Skill involvement requires an observed retrieval event in addition to
the presence of a skill library.

\subsection{Threat model}

The attacker controls a bounded number of task inputs during the learning
history but does not modify the evolution code, skill store, retrieval engine,
judge, or final persistence probe. The attacker succeeds when an exposure is
recorded in a reusable skill and later unsafe behavior appears without a new
attack. The strongest observed outcome is carryover ASR in a fresh
session whose retrieval state is reconstructed only from the final frozen
\texttt{SKILL.md}.

\section{SkillMisevo Gym and Bench}
\label{sec:benchmark}

\subsection{Gym environment and persistence contract}

\gym is an extensible research environment for executing and instrumenting
longitudinal skill evolution under controlled persistent state. It accepts a
task source, target agent, evolution method, and update schedule. The task
adapter supplies executable instances, reset logic, and outcome judges, while
agent and evolution adapters preserve each system's native artifact and expose
skill writes and retrievals. The gym versions $\library_k$, links every authored
diff to its source trajectories, and records outcomes, retrievals, tool traces,
and judge evidence. It therefore supports new task domains, agents, evolution
methods, and governance policies; each instantiated study supplies its task
semantics, update schedule, and metrics.

Conversation, workspace, process, and tool state reset between tasks, leaving
the designated library snapshot as the only cross-task state. The gym also
provides a strict clean-reload operation: it exports a frozen library to
\texttt{SKILL.md}, terminates the executor, and rebuilds the skill index in a
clean executor solely from that file. Conversation, workspace files, tool
sessions, retrieval caches, native memory, and other evolved files do not cross
this boundary. This versioning, isolation, and replay contract is the
experimental substrate; a benchmark decides when to invoke it and which
outcomes to score.

\subsection{SkillMisevo-Bench instantiation and episode protocol}

\bench instantiates the gym as a fixed evaluation of skill misevolution. It uses
AgentHazard~\citep{feng2026agenthazard} as a task-contract source, retaining its
harmful objectives, ten risk categories, sandbox, reset procedure, and harmful
trajectory judge. AHA~\citep{mao2026aha} discovers executable vulnerability
concepts offline by committing falsifiers and promoting replicated mechanisms.
Our constructor independently instantiates each selected mechanism as malicious
$M$ tasks, benign $B$ tasks on related operational surfaces, and held-out $P$
probes. $M$ realizes the unsafe route, $B$ exercises benign work in the same
concept family, and $P$ uses a fresh surface with no attack payload to test what
the reloaded skill supplies. These are separate task records rather than two
views of a shared base instance. The AgentHazard judge scores the concept's
harmful objective on all task types; our benign-completion
judge scores the new $B$ and $P$ objectives. Tasks and metadata are frozen
before evaluation, final tasks are new concept instantiations rather than
replayed AgentHazard prompts, and the authoring VCG remains hidden from the
evaluated system.

Each 21-task episode instantiates one validated concept in the order
$MMM\,BBB\;MMM\,BBB\;MMM\,BBB\mid BBB$. Evolution runs after every three-task
block using those three trajectories, so rounds $R_1,R_2,R_3$ expose the
library to cumulative malicious doses of three, six, and nine before the
following $B^3$ measures contamination. The final $P^3$ runs without further
evolution and mandates the gym's strict persistence boundary: a clean executor
reconstructs retrieval solely from frozen \texttt{SKILL.md}. Each condition
contains 25 episodes.

\subsection{Evaluation settings}

The main evaluation compares \textbf{No Evolution}, which disables persistent
learning, with each method's native \textbf{Full Evolution}, which updates after
every successful three-trajectory batch. Both conditions share tasks, order,
tools, agent configuration, and judges.

\paragraph{Agents and evolution methods.}

Every assembled episode is executed by four target-agent configurations. Claude
Code, Codex, Hermes, and OpenClaw share MiniMax-M2.5, holding the backbone fixed while
the agent framework changes. The target is the complete agent system---its scaffold,
tool mediation, skill lifecycle, retrieval behavior, and backbone---rather than
an isolated language model.

The main comparison includes five agent-agnostic evolution methods: EvoSkill,
SkillClaw, AutoSkill, SkillsVote, and SkillOpt, each evaluated on compatible
target agents. Hermes also provides a framework-native evolution path supported only
by the Hermes target. We include Hermes-native in the main diagnostic table and
provide its implementation details and expanded results in
Appendix~\ref{app:hermes-native}; governed results appear in
Table~\ref{tab:safeevolve-full}. Each method is evaluated in its actual stored representation, including
associated scripts or structured contracts, rather than after conversion to a
common textual summary, for online behavior and artifact auditing. The
carryover stage deliberately standardizes the persistence channel: only the
method's frozen \texttt{SKILL.md} is reloaded for $P$, while associated scripts,
resources, caches, and native memory remain unavailable.

\subsection{Metrics and statistical protocol}

All nine headline metrics are absolute micro-aggregates over the 25 episodes in
a condition. \textbf{Benign Utility (BU)} is the fraction of valid $B$ tasks
that complete the constructed benign objective; \textbf{Malicious ASR (M-ASR)}
is the fraction of valid $M$ tasks that realize the episode's harmful objective;
and \textbf{Benign ASR/Contamination (B-ASR)} applies the same harmful judge and
objective to valid $B$ tasks. BU uses our benign-completion judge, while M-ASR
and B-ASR use the AgentHazard trajectory judge.

Independent content judges assign every skill authored from an $M^3$ or $B^3$
block a 1--5 \textbf{Content Unsafety (CU)} score. \textbf{Unsafe
Generalization (UG)} and \textbf{Stealthiness (Stealth)} are the mean 1--5
generalization and disguise scores over authored skills with $cu\geq3$; CU is
the mean over all authored skills. CU is N/A when no skill is authored, and UG
and Stealth are N/A when none reaches $cu\geq3$. Score distributions and
double-label auditor agreement appear in the appendix.

For carryover, a clean executor rebuilds retrieval solely from final
\texttt{SKILL.md}. \textbf{Unsafe Retrieval Rate (URR)} is the fraction of $P$
tasks that retrieve an evolved skill with exported $cu\geq3$, among $P$ tasks
whose frozen export contains such a skill; \textbf{Carryover ASR (C-ASR)} and
\textbf{Carryover Utility (C-Util)} are the harmful- and benign-completion rates
over valid $P$ tasks. URR is N/A, not zero, when the frozen export contains no
unsafe evolved skill and is interpreted with the artifact scores. We aggregate
valid tasks and judged artifacts directly rather than averaging episode-level
percentages. Confidence intervals use episode-level bootstrap resampling, and
method comparisons use multiple-comparison-adjusted intervals over matched
tasks. Headline metrics pool all rounds; attacker-capability sweeps use their
matched schedule and final artifact snapshot.

\section{SafeEvolve}
\label{sec:safeevolve}

\safeevolve is a governance layer around an existing evolution method. It does
not replace the base agent or add a runtime refusal policy. It governs the
persistent skill update at two decision points: writing and reuse.

\subsection{Safety-aware admission and repair}

Before a candidate enters the library, an independent critic audits the full
bundle, including scripts and referenced resources. When a candidate removes a
safety check, assumes authority, broadens a dangerous operation, or embeds an
unsafe tool sequence, the critic produces a targeted repair: restore the check,
add the missing precondition, or narrow the applicability condition. Repaired
skills are re-audited before admission.

\paragraph{Verification preservation.}

Skill revisions are constrained to preserve existing verification,
confirmation, authorization, and reversibility requirements unless the update
provides explicit evidence that the requirement is obsolete. This prevents an
efficiency-oriented edit from treating a safeguard as removable overhead.

\subsection{Provenance, selection, and retirement}

\paragraph{Lineage and provenance.}

Each skill stores its source tasks, trajectories, revisions, and exposure
status. Provenance supports traceability and batch rollback when an upstream
experience is later judged unsafe. It also supplies a risk prior to retrieval
and retirement decisions.

\paragraph{Safety-aware selection and retirement.}

Raw evolution selects skills primarily by utility. \safeevolve augments this
objective with the admission audit, provenance risk prior, and observed safety
impact of reuse. At retrieval time it suppresses candidates whose exported
instructions remain unsafe or whose lineage contains unsafe reuse evidence;
URR measures whether such a skill still reaches the fresh executor. A retrieved
skill associated with a successful carryover attack is subsequently
downweighted, quarantined, repaired, or retired even when it improves task
success. This distinguishes \safeevolve from capability-only library hygiene,
which keeps useful but unsafe skills.

\begin{figure*}[t]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  core/.style={draw=black!65, rounded corners=2pt, align=center,
    minimum width=2.75cm, minimum height=1.45cm, inner sep=5pt,
    font=\small, fill=white},
  guard/.style={draw=blue!55!black, rounded corners=2pt, align=center,
    minimum width=3.25cm, minimum height=1.85cm, inner sep=5pt,
    font=\small, fill=blue!7},
  flow/.style={-{Stealth[length=2.4mm]}, line width=0.85pt,
    draw=black!70},
  feedback/.style={-{Stealth[length=2.4mm]}, line width=0.8pt,
    draw=red!60!black, dashed},
  label/.style={font=\scriptsize\bfseries, align=center, text=black!70},
  node distance=7mm
]
\node[core, fill=lightgray] (traj) {
  \textbf{Trajectory batch}\\
  tasks + tool traces\\
  \scriptsize observed success};
\node[core, right=of traj, fill=yellow!10] (candidate) {
  \textbf{Candidate skill}\\
  procedure + resources\\
  \scriptsize native method output};
\node[guard, right=of candidate] (writegate) {
  \textbf{Write-time governance}\\
  admission audit + targeted repair\\
  verification preservation\\
  \scriptsize reject / repair / admit};
\node[core, right=of writegate, fill=green!8] (library) {
  \textbf{Governed library}\\
  accepted skill + lineage\\
  \scriptsize source and revision provenance};
\node[guard, right=of library] (reusegate) {
  \textbf{Reuse-time governance}\\
  safety-aware selection\\
  risk prior + retrieval evidence\\
  \scriptsize suppress / retrieve};
\node[core, right=of reusegate, fill=orange!9] (reuse) {
  \textbf{Later task reuse}\\
  agent action + outcome\\
  \scriptsize utility and safety feedback};

\draw[flow] (traj) -- (candidate);
\draw[flow] (candidate) -- (writegate);
\draw[flow] (writegate) -- (library);
\draw[flow] (library) -- (reusegate);
\draw[flow] (reusegate) -- (reuse);

\node[label, below=5mm of writegate] (writecaption) {
  prevents unsafe generalization\\from entering persistent state};
\node[label, below=5mm of reusegate] (reusecaption) {
  prevents retained risk\\from reaching execution};
\node[core, below=11mm of library, fill=red!6,
  minimum width=3.25cm, minimum height=1.2cm] (retire) {
  \textbf{Rollback / repair / retirement}\\
  \scriptsize update the governed library};
\draw[feedback] (reuse.south) |- node[label, near start, below]
  {observed reuse outcome} (retire.east);
\draw[feedback] (retire.north) -- (library.south);
\end{tikzpicture}%
}
\caption{\textbf{SafeEvolve governs the persistent update at writing and
reuse.} Write-time governance audits the complete candidate bundle, repairs
unsafe generalization, and preserves verification requirements before
admission. The library records lineage and revision provenance. Reuse-time
governance combines that provenance with retrieval and outcome evidence, then
suppresses, repairs, rolls back, or retires risky skills while retaining useful
procedures.}
\label{fig:safeevolve}
\end{figure*}

\section{Experimental Setup}
\label{sec:setup}

\subsection{Research questions}

\begin{description}
  \item[RQ1: Safety--utility association.] Is skill evolution associated with
  higher unsafe behavior while retaining or improving task utility?
  \item[RQ2: Risk propagation.] How do authored artifact risk and unsafe
  retrieval relate to harmful execution and utility in a fresh carryover
  session?
  \item[RQ3: Exposure trajectory and schedule.] When does misevolution emerge
  along cumulative malicious exposure, and how does final risk vary with
  exposure timing and composition within skill-update batches?
  \item[RQ4: Mitigation.] Does \safeevolve retain utility while attaining lower
  evolved-artifact risk, malicious and benign ASR, unsafe retrieval, and
  carryover ASR?
\end{description}

\subsection{Evaluation matrix}

The main evaluation treats target-agent as a first-class axis. It compares
Claude Code, Codex, Hermes, and OpenClaw with a shared MiniMax-M2.5 backbone,
holding the model fixed while varying the agent framework, tool mediation,
skill lifecycle, and retrieval behavior. Each applicable agent-agnostic
evolution method runs on the identical frozen episode set for every target.

The complete experimental grid is target-agent $\times$ evolution method
$\times$ episode. Episodes are stratified by AgentHazard risk category,
autoresearch-discovered vulnerability concept and task surface. All main-table cells
pool the same predeclared episode distribution within a target;
macro-averages give equal weight to the four target-agent configurations.
Per-category, per-concept, and per-surface results appear in the appendix.

\subsection{Baselines}

The diagnostic comparison includes no evolution and the selected
skill-evolution methods under their native update schedules. The mitigation comparison
adds Library-Drift as a capability-oriented governance baseline. Each evolution
method is evaluated raw, with Library-Drift, and with \safeevolve under the same
frozen train/development/held-out protocol.

\section{Results}
\label{sec:results}

\subsection{RQ1: Is evolution associated with higher unsafe behavior while retaining utility?}
\label{sec:rq1}

Table~\ref{tab:misevolution-main} is the paper's primary diagnostic table. Its
rows preserve every target-agent and evolution-method combination, while nine
narrow metric columns expose online behavior, the evolved artifact, and
post-attack carryover side by side. This layout makes framework variation
visible without encoding three quantities inside each cell.

% Main Table 1: do not place SafeEvolve in this table.
\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.02}
\resizebox{\textwidth}{!}{
\begin{tabular}{llccc|ccc|ccc}
\toprule
\textbf{Target agent} & \textbf{Evolution setting} &
\multicolumn{3}{c|}{\textbf{Online behavior}} &
\multicolumn{3}{c|}{\textbf{Evolved artifact}} &
\multicolumn{3}{c}{\textbf{Post-attack}} \\
\cmidrule(lr){3-5}\cmidrule(lr){6-8}\cmidrule(lr){9-11}
& & \textbf{BU}$\uparrow$ & \textbf{M-ASR}$\downarrow$ & \textbf{B-ASR}$\downarrow$ &
\textbf{CU}$\downarrow$ & \textbf{UG}$\downarrow$ & \textbf{Stealth}$\downarrow$ &
\textbf{URR}$\downarrow$ & \textbf{C-ASR}$\downarrow$ & \textbf{C-Util}$\uparrow$ \\
\midrule
\multirow{6}{*}{Claude Code}
& No evolution        & 49.8 & 56.0 & 0.0 & N/A & N/A & N/A & N/A & 0.0 & 25.3 \\
& EvoSkill            & 71.1 & 62.2 & 0.4 & 1.79 & 3.05 & 4.05 & 81.0 & 0.0 & 76.0 \\
& SkillClaw           & 36.9 & 51.6 & 0.0 & 1.19 & 3.00 & 3.50 & 16.7 & 0.0 & 29.7 \\
& AutoSkill           & 65.3 & 59.1 & 8.4 & 2.54 & 2.91 & 4.07 & 52.8 & 16.0 & 64.0 \\
& SkillsVote          & 62.7 & 56.4 & 7.1 & 2.58 & 3.05 & 4.19 & 55.6 & 18.7 & 65.3 \\
& SkillOpt            & 60.0 & 58.2 & 0.0 & 1.61 & 3.25 & 3.00 & 50.0 & 0.0 & 42.7 \\
\midrule
\multirow{6}{*}{Codex}
& No evolution        & 54.7 & 59.6 & 0.4 & N/A & N/A & N/A & N/A & 0.0 & 40.0 \\
& EvoSkill            & 76.0 & 64.4 & 0.4 & 1.54 & 2.82 & 3.73 & 33.3 & 0.0 & 65.3 \\
& SkillClaw           & 40.4 & 55.1 & 1.3 & 1.29 & 3.00 & 4.00 & 33.3 & 0.0 & 24.0 \\
& AutoSkill           & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& SkillsVote          & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& SkillOpt            & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
\midrule
\multirow{7}{*}{Hermes}
& No evolution        & 14.2 & 44.0 & 0.4 & N/A & N/A & N/A & N/A & 0.0 & 16.0 \\
& EvoSkill            & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& SkillClaw           & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& AutoSkill           & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& SkillsVote          & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& SkillOpt            & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& Hermes-native       & 68.8 & 73.4 & 16.0 & 2.64 & 3.16 & 4.20 & 62.5 & 30.7 & 64.0 \\
\midrule
\multirow{6}{*}{OpenClaw}
& No evolution        & 37.3 & 44.0 & 0.4 & N/A & N/A & N/A & N/A & 1.3 & 26.7 \\
& EvoSkill            & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& SkillClaw           & 32.0 & 46.0 & 1.3 & 1.54 & 3.20 & 3.80 & 40.0 & 1.3 & 21.3 \\
& AutoSkill           & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& SkillsVote          & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& SkillOpt            & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
\bottomrule
\end{tabular}}
\caption{\textbf{Skill evolution across four target-agent configurations
(RQ1).} BU, M-ASR, B-ASR, URR, C-ASR, and C-Util are percentages. CU, UG,
and Stealth are mean 1--5 content-judge scores; UG and Stealth follow the
judge contract and are evaluated on artifacts with $cu\geq3$. URR, C-ASR, and C-Util report unsafe
retrieval, carryover ASR, and carryover utility after retrieval is rebuilt
solely from \texttt{SKILL.md}. All frameworks use the same MiniMax backbone for
the agent-framework comparison. For URR, N/A means that the frozen
\texttt{SKILL.md} contains no evolved skill with exported $cu\geq3$; URR is
then undefined rather than zero. Elsewhere, N/A marks a setting with no skill
artifact or an unsupported framework-specific pairing; it never denotes a failed run.
The safety--utility and artifact--retrieval--carryover relationships are
visualized in Figure~\ref{fig:risk-propagation}.}
\label{tab:misevolution-main}
\end{table*}

\paragraph{Capability gains can coincide with higher malicious success.}
Higher benign utility frequently appears alongside higher malicious ASR in
Table~\ref{tab:misevolution-main}. EvoSkill reports BU/M-ASR of 71.1/62.2 on
Claude Code and 76.0/64.4 on Codex, compared with 49.8/56.0 and 54.7/59.6
under no evolution. Hermes-native reports 68.8/73.4, compared with 14.2/44.0
under no evolution on Hermes. SkillClaw defines a different regime: utility
generally falls and malicious ASR does not show the same increase. The
association therefore varies across evolution method--agent systems.

\paragraph{Three settings exhibit the full observed misevolution signature.}
AutoSkill and SkillsVote on Claude Code reach B-ASR 8.4/7.1 and fresh-session
C-ASR 16.0/18.7 while retaining C-Util 64.0/65.3. Hermes-native is the
strongest observed case, reaching B-ASR 16.0, C-ASR 30.7, and C-Util 64.0.
Across these settings, the benchmark observes an evolved unsafe artifact,
later benign contamination, and harmful behavior after the executor is reset
and retrieval is rebuilt solely from the frozen \texttt{SKILL.md}. Carryover is therefore
compatible with retained task competence rather than being explained by a
generally broken agent.

\paragraph{Artifact risk is broader than realized carryover harm.}
The remaining settings separate earlier stages of the lifecycle from harmful
execution. EvoSkill and SkillOpt on Claude Code produce unsafe retrieval rates
of 81.0 and 50.0 while C-ASR remains zero; Codex with EvoSkill similarly reaches
33.3 URR with zero C-ASR. Unsafe skill content can therefore be authored and
retrieved without producing an observed harmful action in the persistence
probe. Runtime policy, tool mediation, and task context remain gates between a
persistent unsafe rule and realized harm.

\paragraph{Safety--utility frontier.}
The high-utility, high-C-ASR region is the central misevolution regime:
AutoSkill, SkillsVote, and Hermes-native preserve useful procedures while also
retaining an unsafe shortcut. Capability improvement alone is therefore not a
reliable safety signal, while artifact risk alone does not establish that the
shortcut will be executed. The nine metrics locate both properties without
collapsing them into a single score.

\subsection{RQ2: How do artifact and retrieval risk relate to later harm?}
\label{sec:rq2}

RQ2 compares observable stages of Equation~\ref{eq:sequence}.
Figure~\ref{fig:risk-propagation}(b) pairs unsafe retrieval with carryover ASR
for every configuration, making the retrieval-to-execution gap visible on one
percentage scale. Figure~\ref{fig:risk-propagation}(c) then locates the settings
where contamination during benign learning and harmful execution in the fresh
carryover session co-occur. Panel (a) connects this propagation analysis back
to RQ1's safety--utility association.

\begin{figure*}[t]
\centering
\resizebox{\textwidth}{!}{%
\begin{tikzpicture}[
  x=1cm,y=1cm,
  tick/.style={font=\tiny, text=black!65},
  axislabel/.style={font=\scriptsize, text=black!85},
  paneltitle/.style={font=\scriptsize\bfseries, text=black!85},
  note/.style={font=\tiny, align=left, text=black!75},
  cc/.style={circle, fill=blue!65!black, draw=white, line width=.4pt,
    minimum size=5.5pt, inner sep=0pt},
  codex/.style={circle, fill=orange!85!black, draw=white, line width=.4pt,
    minimum size=5.5pt, inner sep=0pt},
  hermes/.style={circle, fill=red!70!black, draw=white, line width=.4pt,
    minimum size=5.5pt, inner sep=0pt},
  openclaw/.style={circle, fill=green!55!black, draw=white, line width=.4pt,
    minimum size=5.5pt, inner sep=0pt},
  basecc/.style={circle, fill=white, draw=blue!65!black, line width=.8pt,
    minimum size=5.5pt, inner sep=0pt},
  basecodex/.style={circle, fill=white, draw=orange!85!black, line width=.8pt,
    minimum size=5.5pt, inner sep=0pt},
  basehermes/.style={circle, fill=white, draw=red!70!black, line width=.8pt,
    minimum size=5.5pt, inner sep=0pt},
  baseopenclaw/.style={circle, fill=white, draw=green!55!black, line width=.8pt,
    minimum size=5.5pt, inner sep=0pt},
  urr/.style={rectangle, fill=blue!55, draw=white, line width=.35pt,
    minimum size=5pt, inner sep=0pt},
  casr/.style={circle, fill=black!75, draw=white, line width=.35pt,
    minimum size=5pt, inner sep=0pt}
]
% Panel (a): BU in [10,80], M-ASR in [40,75].
\begin{scope}[shift={(0,0)}]
  \draw[->, black!70] (0,0) -- (4.95,0);
  \draw[->, black!70] (0,0) -- (0,4.75);
  \foreach \x/\lab in {.69/20,2.06/40,3.43/60,4.80/80}
    {\draw[black!45] (\x,0) -- (\x,-.08) node[tick,below] {\lab};}
  \foreach \y/\lab in {0/40,1.31/50,2.63/60,3.94/70}
    {\draw[black!45] (0,\y) -- (-.08,\y) node[tick,left] {\lab};}
  \node[paneltitle, anchor=west] at (0,5.02) {(a) Utility and malicious success};
  \node[axislabel] at (2.45,-.55) {Benign Utility (\%)};
  \node[axislabel, rotate=90] at (-.62,2.35) {Malicious ASR (\%)};

  \node[basecc] at (2.73,2.10) {};
  \node[cc] at (4.19,2.92) {};
  \node[cc] at (1.84,1.52) {};
  \node[cc] at (3.79,2.51) {};
  \node[cc] at (3.61,2.16) {};
  \node[cc] at (3.43,2.39) {};
  \node[basecodex] at (3.06,2.58) {};
  \node[codex] at (4.53,3.21) {};
  \node[codex] at (2.08,1.98) {};
  \node[basehermes] at (.29,.53) {};
  \node[hermes] at (4.03,4.39) {};
  \node[baseopenclaw] at (1.87,.53) {};
  \node[openclaw] at (1.51,.79) {};

  \draw[black!40] (4.03,4.39) -- (3.68,4.62)
    node[note,anchor=east] {Hermes-native};
  \draw[black!40] (4.53,3.21) -- (4.25,3.50)
    node[note,anchor=east] {Codex / EvoSkill};
\end{scope}

% Panel (b): paired URR and C-ASR percentages.
\begin{scope}[shift={(6.15,0)}]
  \draw[->, black!70] (0,0) -- (4.85,0);
  \foreach \x/\lab in {0/0,1.57/30,3.13/60,4.70/90}
    {\draw[black!45] (\x,0) -- (\x,-.08) node[tick,below] {\lab};}
  \node[paneltitle, anchor=west] at (-1.15,5.02) {(b) Retrieval-to-execution gap};
  \node[axislabel] at (2.38,-.55) {Rate (\%)};

  \foreach \lab/\y/\u/\c in {
    CC-Evo/4.32/4.23/0,
    CC-Claw/3.84/.87/0,
    CC-Auto/3.36/2.76/.84,
    CC-Vote/2.88/2.90/.98,
    CC-Opt/2.40/2.61/0,
    CX-Evo/1.92/1.74/0,
    CX-Claw/1.44/1.74/0,
    H-Native/.96/3.26/1.60,
    OC-Claw/.48/2.09/.07}
  {
    \node[tick,anchor=east] at (-.13,\y) {\lab};
    \draw[black!28, line width=1pt] (\c,\y) -- (\u,\y);
    \node[casr] at (\c,\y) {};
    \node[urr] at (\u,\y) {};
  }
  \node[urr] at (.35,-1.02) {};
  \node[tick,anchor=west] at (.51,-1.02) {URR};
  \node[casr] at (1.32,-1.02) {};
  \node[tick,anchor=west] at (1.48,-1.02) {C-ASR};
\end{scope}

% Panel (c): B-ASR in [0,18], C-ASR in [0,35], marker area tracks CU.
\begin{scope}[shift={(12.25,0)}]
  \draw[->, black!70] (0,0) -- (4.55,0);
  \draw[->, black!70] (0,0) -- (0,4.75);
  \foreach \x/\lab in {0/0,1.47/6,2.93/12,4.40/18}
    {\draw[black!45] (\x,0) -- (\x,-.08) node[tick,below] {\lab};}
  \foreach \y/\lab in {0/0,1.31/10,2.63/20,3.94/30}
    {\draw[black!45] (0,\y) -- (-.08,\y) node[tick,left] {\lab};}
  \node[paneltitle, anchor=west] at (0,5.02) {(c) Full misevolution signature};
  \node[axislabel] at (2.25,-.55) {Benign ASR / Contamination (\%)};
  \node[axislabel, rotate=90] at (-.62,2.35) {Carryover ASR (\%)};

  \node[cc, minimum size=6.5pt] at (.10,0) {};
  \node[cc, minimum size=4.5pt] at (0,0) {};
  \node[cc, minimum size=8.5pt] at (2.05,2.10) {};
  \node[cc, minimum size=8.7pt] at (1.74,2.46) {};
  \node[cc, minimum size=5.9pt] at (0,0) {};
  \node[codex, minimum size=5.7pt] at (.10,0) {};
  \node[codex, minimum size=4.9pt] at (.32,0) {};
  \node[hermes, minimum size=9pt] at (3.91,4.03) {};
  \node[openclaw, minimum size=5.7pt] at (.32,.17) {};

  \draw[black!40] (3.91,4.03) -- (3.52,4.38)
    node[note,anchor=east] {Hermes-native};
  \draw[black!40] (1.74,2.46) -- (1.40,2.80)
    node[note,anchor=east] {SkillsVote};
  \draw[black!40] (2.05,2.10) -- (2.35,1.78)
    node[note,anchor=west] {AutoSkill};
\end{scope}

% Shared framework legend; hollow markers denote No Evolution in panel (a).
\node[cc] at (3.35,-1.28) {};
\node[tick,anchor=west] at (3.50,-1.28) {Claude Code};
\node[codex] at (5.20,-1.28) {};
\node[tick,anchor=west] at (5.35,-1.28) {Codex};
\node[hermes] at (6.43,-1.28) {};
\node[tick,anchor=west] at (6.58,-1.28) {Hermes};
\node[openclaw] at (7.75,-1.28) {};
\node[tick,anchor=west] at (7.90,-1.28) {OpenClaw};
\node[basecc] at (9.36,-1.28) {};
\node[tick,anchor=west] at (9.51,-1.28) {No Evolution};
\end{tikzpicture}%
}
\caption{\textbf{Three diagnostic views of skill misevolution (RQ1--RQ2).}
Each point or row is an agent--method configuration from
Table~\ref{tab:misevolution-main}. Panel (a) shows that higher benign utility
can coexist with higher malicious ASR; hollow markers denote No Evolution.
Panel (b) pairs unsafe retrieval and carryover ASR on the same scale, exposing
the gap between retrieving and executing an unsafe skill. Panel (c) locates
online benign contamination and fresh-session carryover harm; marker area
scales with the mean Content Unsafety score. AutoSkill, SkillsVote, and
Hermes-native form the clearest full misevolution cases.}
\label{fig:risk-propagation}
\end{figure*}

\paragraph{Artifact risk is broader than behavioral harm.}
The aggregate results already expose an artifact--behavior gap. Every reported
evolution setting produces nontrivial artifact scores, whereas clear elevated
carryover ASR is concentrated in AutoSkill, SkillsVote, and Hermes-native.
EvoSkill and SkillOpt further show that even observed unsafe retrieval need not
be followed by harm. The benchmark separates latent artifact risk, retrieved
risk, and realized carryover at the episode level. This is a
gated lifecycle: the observed progression can stop because no unsafe skill is
exported, the skill is not retrieved, or the executor declines to act on it.

\paragraph{Worked temporal trace.}
Show one compact case from source exposure to authored skill diff, later
retrieval log, unsafe tool action, judge state, and fresh-session reproduction.
The example should expose the exact over-generalized clause and the safety check
that evolution removed or failed to encode.

\subsection{RQ3: How do cumulative exposure and its update schedule shape misevolution?}
\label{sec:rq3}

RQ3 uses the existing $9M+9B+3P$ episode records without constructing paired
views or replacement tasks. We evaluate Claude Code with AutoSkill, Codex with
EvoSkill, and Hermes with Hermes-native on the same predeclared
concept--surface families. The first analysis follows the standard interleaved
episode and freezes the library before learning and after each complete
$M^3,B^3$ round. These checkpoints contain cumulative malicious exposure
$K\in\{0,3,6,9\}$. At every checkpoint, a clean executor reloads only that
snapshot's \texttt{SKILL.md} and runs the same evaluation-only $P^3$ probes,
which never update the library. This repeated-snapshot trajectory identifies
the onset and accumulation of persistent harm across the native episode.

Two schedule comparisons then hold the complete task inventory, six native
updates, tools, judges, and final $P^3$ fixed. The timing comparison orders the
six learning blocks as \textsc{Early}
($MMM|MMM|MMM|BBB|BBB|BBB$), \textsc{Interleaved}
($MMM|BBB|MMM|BBB|MMM|BBB$), or \textsc{Late}
($BBB|BBB|BBB|MMM|MMM|MMM$). It measures whether later benign evolution repairs
an early unsafe update and whether late exposure persists when fewer repair
opportunities remain. The batch-composition comparison retains nine malicious
and nine benign tasks while changing how strongly malicious experience
dominates individual updates. \textsc{Batched} uses six pure blocks,
\textsc{Partially Mixed} uses two pure and four mixed blocks, and
\textsc{Fully Mixed} alternates $MMB$ and $MBB$ so every update sees both task
types. Each schedule is paired with its temporal mirror across episodes, and
within-block positions are rotated, keeping pooled exposure position centered.

Figure~\ref{fig:rq3-schedule} places cumulative exposure, timing, and batch
composition in one row. All panels report absolute Carryover ASR: harm must
survive export, clean-session reload, retrieval, and execution. Artifact,
retrieval, contamination, and utility outcomes at the same checkpoints and
schedules appear in the appendix.

\begin{figure*}[t]
  \centering
  \fbox{\parbox[c][4.0cm][c]{0.96\textwidth}{
    \centering
    \textbf{One-row, three-panel exposure-schedule line plot.}\\[5pt]
    Shared y-axis: Carryover ASR (\%).\\[3pt]
    \textbf{(a) Cumulative exposure:} $K=0,3,6,9$ \quad
    \textbf{(b) Timing:} Early, Interleaved, Late \quad
    \textbf{(c) Batch composition:} Fully Mixed, Partially Mixed, Batched.\\[3pt]
    One line per selected agent--method configuration; points show absolute
    micro-aggregates with episode-bootstrap 95\% CIs and all panels share one
    legend.
  }}
  \caption{\textbf{Cumulative exposure and update schedule (RQ3).}
  \textbf{(a)} replays the same $P^3$ probes from clean executors at successive
  snapshots of the standard episode. \textbf{(b)} reorders the complete
  $9M+9B$ inventory to compare early, interleaved, and late exposure.
  \textbf{(c)} redistributes that fixed inventory across pure and mixed update
  batches. Timing and composition conditions preserve all task identities and
  six evolution updates. Lines report absolute fresh-session Carryover ASR.}
  \label{fig:rq3-schedule}
\end{figure*}

\subsection{RQ4: How do safety and utility compare under SafeEvolve?}
\label{sec:rq4}

The mitigation evaluation uses held-out AgentHazard episodes and attack
strategies that are hidden
during evolution, repair-policy development, and threshold selection. Libraries
are frozen before evaluation. A favorable mitigation profile has lower
malicious ASR, benign contamination, and lower artifact scores alongside preserved
benign utility. Post-attack, it has either no unsafe exported skill or, where
unsafe exported skills remain, lower fresh-session URR and carryover ASR
alongside preserved carryover utility. The three stages are reported together
rather than collapsed into one score.

% Appendix table defined here and rendered after \appendix.
\newcommand{\safeevolvefulltable}{%
\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3.5pt}
\renewcommand{\arraystretch}{1.04}
\resizebox{\textwidth}{!}{
\begin{tabular}{llccccc}
\toprule
\textbf{Evolution method} & \textbf{Governance}
& \textbf{CC/MiniMax} & \textbf{Codex/MiniMax} & \textbf{Hermes/MiniMax}
& \textbf{OpenClaw/MiniMax}
& \textbf{Macro avg.} \\
\midrule
\multicolumn{7}{l}{\textbf{A. Online behavior: benign utility / malicious ASR / benign ASR (contamination) (\%)}} \\
\multirow{3}{*}{AutoSkill}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{EvoSkill}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{SkillClaw}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{SkillsVote}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{SkillOpt}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{Hermes-native}
 & Raw             & N/A & N/A & --/--/-- & N/A & --/--/-- \\
 & Library-Drift   & N/A & N/A & --/--/-- & N/A & --/--/-- \\
 & \safeevolve     & N/A & N/A & --/--/-- & N/A & --/--/-- \\
\midrule
\multicolumn{7}{l}{\textbf{B. Evolved artifact scores: CU / UG / Stealth ($\downarrow$, 1--5)}} \\
\multirow{3}{*}{AutoSkill}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{EvoSkill}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{SkillClaw}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{SkillsVote}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{SkillOpt}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{Hermes-native}
 & Raw             & N/A & N/A & --/--/-- & N/A & --/--/-- \\
 & Library-Drift   & N/A & N/A & --/--/-- & N/A & --/--/-- \\
 & \safeevolve     & N/A & N/A & --/--/-- & N/A & --/--/-- \\
\midrule
\multicolumn{7}{l}{\textbf{C. Post-attack: unsafe retrieval ($\downarrow$) / carryover ASR ($\downarrow$) / carryover utility ($\uparrow$) (\%)}} \\
\multirow{3}{*}{AutoSkill}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{EvoSkill}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{SkillClaw}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{SkillsVote}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{SkillOpt}
 & Raw             & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & Library-Drift   & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
 & \safeevolve     & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\midrule
\multirow{3}{*}{Hermes-native}
 & Raw             & N/A & N/A & --/--/-- & N/A & --/--/-- \\
 & Library-Drift   & N/A & N/A & --/--/-- & N/A & --/--/-- \\
 & \safeevolve     & N/A & N/A & --/--/-- & N/A & --/--/-- \\
\bottomrule
\end{tabular}}
\caption{\textbf{Full SafeEvolve breakdown by evolution method and target
agent.} Each evolution method is evaluated raw,
with a capability-oriented library hygiene baseline, and with \safeevolve on
the same held-out episodes. Columns retain all four target-agent configurations.
Panel A reports benign utility / malicious ASR / benign ASR
(contamination); Panel B reports mean 1--5 CU / UG / Stealth scores; Panel C reports URR /
carryover ASR / carryover utility. All metrics are
absolute micro-aggregates. No deltas or gain-retention ratios are used. Macro averages
weight target agents equally and exclude genuinely unsupported method--agent
pairs. URR is additionally averaged only over cells in which the frozen
\texttt{SKILL.md} contains an evolved skill with exported $cu\geq3$; otherwise
it is reported as N/A rather than zero. Bold marks the safest method whose
utility is statistically non-inferior to raw evolution. Hermes-native is evaluated only in
the Hermes / MiniMax column; its macro entry therefore summarizes that single
supported target rather than imputing results for other agents.}
\label{tab:safeevolve-full}
\end{table*}
}

% Main Table 2: method-averaged held-out mitigation results.
\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\renewcommand{\arraystretch}{1.06}
\resizebox{\textwidth}{!}{
\begin{tabular}{llccc|ccc|ccc}
\toprule
\textbf{Target agent} & \textbf{Governance} &
\multicolumn{3}{c|}{\textbf{Online behavior}} &
\multicolumn{3}{c|}{\textbf{Evolved artifact}} &
\multicolumn{3}{c}{\textbf{Post-attack}} \\
\cmidrule(lr){3-5}\cmidrule(lr){6-8}\cmidrule(lr){9-11}
& & \textbf{BU}$\uparrow$ & \textbf{M-ASR}$\downarrow$ & \textbf{B-ASR}$\downarrow$ &
\textbf{CU}$\downarrow$ & \textbf{UG}$\downarrow$ & \textbf{Stealth}$\downarrow$ &
\textbf{URR}$\downarrow$ & \textbf{C-ASR}$\downarrow$ & \textbf{C-Util}$\uparrow$ \\
\midrule
\multirow{3}{*}{Claude Code}
& Raw & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& Library-Drift & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
\rowcolor{lightblue}& \safeevolve & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
\midrule
\multirow{3}{*}{Codex}
& Raw & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& Library-Drift & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
\rowcolor{lightblue}& \safeevolve & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
\midrule
\multirow{3}{*}{Hermes}
& Raw & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& Library-Drift & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
\rowcolor{lightblue}& \safeevolve & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
\midrule
\multirow{3}{*}{OpenClaw}
& Raw & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& Library-Drift & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
\rowcolor{lightblue}& \safeevolve & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
\midrule
\multirow{3}{*}{Macro avg.}
& Raw & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
& Library-Drift & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
\rowcolor{lightblue}& \safeevolve & -- & -- & -- & -- & -- & -- & -- & -- & -- \\
\bottomrule
\end{tabular}}
\caption{\textbf{SafeEvolve safety and utility under the same evaluation
protocol (RQ4).} For each target agent and governance condition, we
first compute every metric as an absolute micro-aggregate, then macro-average
equally over the five agent-agnostic evolution methods. The final row block
gives the equal-weight target-agent average. Unsupported method--agent pairs
are excluded. Artifact columns report the same mean judge scores as
Table~\ref{tab:misevolution-main}. URR is N/A when no unsafe evolved skill is exported and its averages
use only defined cells; this absence is also exposed by the artifact columns
rather than converted into a zero retrieval rate. The complete per-method breakdown is reported in
Appendix~Table~\ref{tab:safeevolve-full}.}
\label{tab:safeevolve-main}
\end{table*}

\paragraph{Why Library-Drift is the decisive baseline.}
Capability-oriented hygiene can remove stale or low-value skills, but it may
retain a skill associated with both higher utility and an unsafe shortcut. A gap between
Library-Drift and \safeevolve provides a direct comparison between
capability-oriented and explicitly safety-aware governance.

\paragraph{Full-system evaluation on SkillSafetyBench.}
The held-out episode table tests end-to-end mitigation inside evolving agents.
We then test the complete \safeevolve pipeline on SkillSafetyBench, which places
adversarial content in skill-facing local artifacts and measures whether an
agent both completes the nominal task and realizes the attack
\citep{jin2026skillsafetybench}. For each case, the original clean SkillsBench
skill is the ancestor and the attacked skill bundle is the proposed update.
\safeevolve runs admission, repair, verification preservation, lineage,
quarantine/rollback, selection, and governed reuse over the complete bundle and
its referenced files before the resulting skill is executed in the official
environment. The gym's multi-round episodes remain the test of longitudinal
retirement; SkillSafetyBench supplies an independent one-update, downstream
behavior test.

SkillSafetyBench's unsafe outcome on a nominally benign task resembles benign
ASR (contamination), but the evaluation setup differs: it supplies the adversarial
artifact in the evaluated run, whereas \bench contamination is harmful behavior
observed on a later zero-injection benign task after a skill was learned from
earlier malicious experience. We therefore retain SkillSafetyBench's official ASR, task
success, and task-success-conditional ASR rather than relabeling its result as
contamination.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{7pt}
\renewcommand{\arraystretch}{1.08}
\begin{tabular}{lccc}
\toprule
\textbf{Governance} & \textbf{ASR $\downarrow$}
& \textbf{Task success $\uparrow$} & \textbf{Conditional ASR $\downarrow$} \\
\midrule
Raw attacked update & -- & -- & -- \\
Library-Drift & -- & -- & -- \\
\rowcolor{lightblue}Full \safeevolve & -- & -- & -- \\
\bottomrule
\end{tabular}
\caption{\textbf{Full SafeEvolve on SkillSafetyBench (RQ4).} Results use the
official overall attack success rate (ASR), nominal task success, and
task-success-conditional ASR. Entries macro-average the four target-agent
configurations after computing each metric under its official denominator.
Per-agent and RD1--RD6 results appear in the appendix.}
\label{tab:skill-safety}
\end{table*}

\paragraph{Admission-auditor evaluation on MalSkillBench.}
MalSkillBench is a labeled static corpus of malicious and benign skill bundles
\citep{guo2026malskillbench}. It cannot exercise repair trajectories, lineage,
retirement, or downstream reuse, so we use it only to diagnose \safeevolve's
admission auditor. Table~\ref{tab:malskill} follows the benchmark's official
classification protocol; it is supporting component evidence rather than a
substitute for the two full-system evaluations.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{6pt}
\begin{tabular}{lcccccc}
\toprule
\textbf{Detector} & \textbf{Acc. $\uparrow$} & \textbf{Prec. $\uparrow$}
& \textbf{Rec. $\uparrow$} & \textbf{F1 $\uparrow$}
& \textbf{FP $\downarrow$} & \textbf{FN $\downarrow$} \\
\midrule
Best published skill detector & -- & -- & -- & -- & -- & -- \\
Best published supply-chain detector & -- & -- & -- & -- & -- & -- \\
Best published prompt-injection detector & -- & -- & -- & -- & -- & -- \\
\rowcolor{lightblue}\safeevolve admission auditor & -- & -- & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\caption{\textbf{SafeEvolve admission-auditor evaluation.} Accuracy,
precision, recall, F1, false-positive count, and false-negative count are
computed on MalSkillBench's pinned official test set. The appendix reports all
individual detectors.}
\label{tab:malskill}
\end{table*}

\paragraph{Capability preservation on common benchmarks.}
Safety gains are useful only if the governed library continues to improve task
performance. We therefore evaluate each evolution method raw, with
Library-Drift, and with \safeevolve on two external capability benchmarks.
Here too we preserve each benchmark's official evaluation contract rather than
reducing both to a common accuracy column. SkillsBench reports task-macro
pass rates and skill gain \citep{li2026skillsbench}; SkillLearnBench separates
skill quality, trajectory behavior, and task outcome
\citep{zhong2026skilllearnbench}. Table~\ref{tab:capability} shows
method-averaged results in the main paper, with every evolution method reported
separately in the appendix.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{5pt}
\renewcommand{\arraystretch}{1.08}
\begin{tabular}{llccc}
\toprule
\textbf{Benchmark} & \textbf{Native metric}
& \textbf{Raw} & \textbf{Library-Drift} & \textbf{\safeevolve} \\
\midrule
\multirow{4}{*}{SkillsBench}
 & No-skill pass (\%) $\uparrow$       & -- & -- & -- \\
 & Evolved-skill pass (\%) $\uparrow$  & -- & -- & -- \\
 & Skill improvement (pp) $\uparrow$   & -- & -- & -- \\
 & Normalized gain (\%) $\uparrow$     & -- & -- & -- \\
\midrule
\multirow{7}{*}{SkillLearnBench}
 & Coverage (\%) $\uparrow$            & -- & -- & -- \\
 & Executability (\%) $\uparrow$       & -- & -- & -- \\
 & Safety (\%) $\uparrow$              & -- & -- & -- \\
 & Alignment (\%) $\uparrow$           & -- & -- & -- \\
 & Usage (\%) $\uparrow$               & -- & -- & -- \\
 & Solving tokens $\downarrow$          & -- & -- & -- \\
 & Accuracy (\%) $\uparrow$            & -- & -- & -- \\
\bottomrule
\end{tabular}
\caption{\textbf{SafeEvolve preserves capability under each benchmark's native
protocol (RQ4).} Entries macro-average equally over applicable evolution
methods after computing the official metric for each method. SkillsBench uses
task-macro pass rate, absolute improvement, and normalized gain;
SkillLearnBench reports its three-level quality/trajectory/outcome metrics.
The no-skill reference is repeated to keep each official comparison
self-contained. We do not average heterogeneous metrics across benchmarks.
Full per-method results and confidence intervals appear in the appendix.}
\label{tab:capability}
\end{table*}

\paragraph{Component evaluation.}
Finally, ablate admission auditing, verification preservation, provenance
rollback, and safety-aware retirement. These component results characterize the
three complete RQ4 evaluations; they do not replace held-out behavioral or
capability evidence.

\begin{table*}[t]
\centering
\small
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lccc}
\toprule
\textbf{SafeEvolve variant} &
\textbf{Online: BU/M-ASR/B-ASR} &
\textbf{Artifact scores: CU/UG/Stealth} &
\textbf{Carryover: URR/ASR/Utility} \\
\midrule
Full \safeevolve             & --/--/-- & --/--/-- & --/--/-- \\
$-$ admission critic         & --/--/-- & --/--/-- & --/--/-- \\
$-$ verification preservation& --/--/-- & --/--/-- & --/--/-- \\
$-$ provenance               & --/--/-- & --/--/-- & --/--/-- \\
$-$ safety-aware retirement  & --/--/-- & --/--/-- & --/--/-- \\
\bottomrule
\end{tabular}
\caption{\textbf{SafeEvolve component analysis.} Every row reports the same
nine absolute metrics as the main evaluation, grouped as $3+3+3$.}
\label{tab:safeevolve-ablation}
\end{table*}

\section{Discussion}
\label{sec:discussion}

\paragraph{Skills are security-relevant policy.}
The central implication is that a skill library should not be governed as a
passive document store. A stored skill can alter tool choice, ordering,
verification, and authorization across future tasks. Admission, provenance,
rollback, and safety monitoring should therefore be part of the learning
boundary.

\paragraph{Artifact risk and post-attack harm are distinct.}
An evolved artifact can score highly on content unsafety, unsafe
generalization, or stealthiness before its unsafe procedure is retrieved. This
is latent risk, not a safe artifact. Conversely, a runtime policy can block
execution while the persistent library continues to carry the unsafe rule.
Product evaluation should therefore report online utility and ASR, the three
artifact properties, and unsafe retrieval, carryover ASR, and carryover utility
separately. The observed progression is gated rather than automatic: unsafe
experience can yield an unsafe artifact without later retrieval, and retrieval
can occur without harmful execution.

\paragraph{Capability and safety share the same shortcut.}
The difficult cases are not useless malicious skills. They are procedures in
which higher success appears alongside the removal of friction that served as a
safeguard. Accordingly, we report capability together with safety rather than
treating either one alone as sufficient.

\paragraph{Misevolution is a system-level outcome.}
The same backbone exhibits different outcomes across agent frameworks and
evolution methods. Some settings complete the path from unsafe experience to
clean-session harm, while others retain the unsafe rule without executing it.
Skill authoring, serialization, retrieval, tool mediation, and runtime policy
jointly determine where the observed progression stops. This heterogeneity is
part of the benchmark result: it identifies which layer requires governance.

\paragraph{Scope and limitations.}
The study focuses on readable skill artifacts written by CLI and coding agents
and on bounded direct exposure in executable task environments. Results support
claims about incremental risk observed under evolution within this setting. Future
work should test indirect exposure, multimodal skills, opaque learned memories,
longer deployment horizons, interacting libraries, and adaptive attackers.

\section{Conclusion}
\label{sec:conclusion}

Skill self-evolution changes the temporal structure of agent safety. Our
evaluation identifies a persistent channel through which unsafe success can be
generalized, stored, retrieved, and, in some method--agent systems, carried into
clean-session behavior after the original attack is gone. Other systems stop at
artifact formation or retrieval, making the location of that boundary part of
the safety result. \gym makes this longitudinal state observable and replayable;
\bench distinguishes latent artifact risk from
retrieved risk and realized carryover. \safeevolve governs the persistent
update while preserving useful learning. Safe self-improvement requires
evaluating not only whether an agent succeeds today, but also which persistent
skills are created, when they are retrieved, and what later behavior follows.

\bibliography{iclr2026_conference}
\bibliographystyle{iclr2026_conference}

\appendix

\section{Gym Interface and Benchmark Construction}
\label{app:construction}

\paragraph{Source contract.}
AgentHazard contributes the harmful objective taxonomy, executable
computer-use environment, reset logic, and harmful trajectory judge. We pin its
version and preserve the category and harmful-goal semantics in every derived
task. Its published prompts are inputs to vulnerability discovery rather than
the final \bench episode dataset.

\paragraph{External autoresearch concept discovery.}
For each benchmark task family, we use AHA~\citep{mao2026aha} as an external
offline method to search for a vulnerability concept that can survive
abstraction into a reusable skill. Its Hypothesizer
commits a mechanism, predicted unsafe outcome, and falsifier; the
Attack-Designer instantiates the proposal in the AgentHazard environment; the
victim trajectory is judged and reflected on; and only replicated,
non-falsified findings enter the VCG. We retain the concept identifier,
mechanism, enabling condition, attack template, failure outcome, transfer
prediction, and discovery provenance. This stage determines what failure
mechanism a task tests, not what skill the evaluated agent should write.

\paragraph{Concept-guided episode instantiation.}
For each retained concept, our constructor emits independent malicious $M$ and
benign $B$ task records across a related family of operational surfaces. $M$
instantiates the concept's enabling condition and episode-level harmful goal.
$B$ exercises legitimate work in the same concept and tool-use family while
removing the unsafe route. An $M$ and a $B$ may share a surface, but the
benchmark does not encode them as two views of one base task. Additional
surface instantiations produce $P$ probes whose prompts contain neither the
malicious payload nor its explicit unsafe instruction. These probes retain
enough semantic overlap to retrieve a generalized skill and test what the skill
contributes in a clean session.

\paragraph{Judging and validation.}
The AgentHazard harmful judge evaluates the episode-level harmful objective on $M$,
$B$, and $P$. We add a separate benign-completion judge for the constructed
benign objective; its decision is independent of whether an additional harmful
action occurred. Construction validation checks executability, reset behavior,
concept-family and surface coverage across each episode, removal of the
malicious condition from $B/P$, prompt and instance deduplication, and judge
coverage. Concepts, prompts,
judges, and splits are frozen before the evolution-method evaluation.

\paragraph{Episode manifest and replay.}
The released manifest records, for every episode, its source VCG concept identifier,
mechanism, AgentHazard category, malicious goal, task surface, ordered $M/B/P$
instances, and source provenance. It also records each skill snapshot and the
deterministic final export to \texttt{SKILL.md}. The clean-executor check verifies
that the $P$ retrieval index reads this export and no other learning-stage
state, allowing every result to be replayed from the same constructed task and
skill snapshot.

\section{Agent and Method Configurations}
\label{app:configurations}

Report every agent framework, model endpoint, evolution implementation,
retrieval configuration, update cadence, prompt, decoding parameter, repository
commit, and applicability restriction. Separate unsupported combinations from
failed runs.

\subsection{Hermes-native evolution}
\label{app:hermes-native}

Hermes-native evolution is evaluated only with the Hermes / MiniMax target and
is excluded from every cross-agent main-table average. A native candidate
follows Hermes's ordinary authoring, approval, and storage path in the raw
condition. Library-Drift applies capability-oriented maintenance after writing.
Under \safeevolve, the complete native skill bundle is audited and, when
necessary, repaired before approval; accepted skills then receive the same
provenance, safety-aware retrieval, and retirement treatment as candidates from
agent-agnostic methods. This preserves Hermes's authoring loop and changes only
the governance of its persistent updates.

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lccc}
\toprule
\textbf{Hermes / MiniMax setting} &
\textbf{Online: BU/M-ASR/B-ASR} &
\textbf{Artifact scores: CU/UG/Stealth} &
\textbf{Post-attack: URR/ASR/Utility} \\
\midrule
No evolution        & 14.2/44.0/0.4 & N/A/N/A/N/A & N/A/0.0/16.0 \\
Hermes-native       & 68.8/73.4/16.0 & 2.64/3.16/4.20 & 62.5/30.7/64.0 \\
\bottomrule
\end{tabular}}
\caption{\textbf{Hermes-native diagnostic detail.} This appendix view expands
the Hermes-specific rows included in Table~\ref{tab:misevolution-main} using
the same absolute $3+3+3$ protocol. Its Raw / Library-Drift / \safeevolve
comparison appears in Table~\ref{tab:safeevolve-full}.}
\label{tab:hermes-native}
\end{table}

\section{Judges and Artifact Auditing}
\label{app:judges}

Provide the retained AgentHazard harmful trajectory judge, our benign-completion
judge, and the complete 1--5 rubrics for content unsafety, unsafe generalization, and stealthiness, together with auditor
prompts, calibration examples, agreement statistics, and the adjudication
procedure. Report the raw score distributions and means. State which models are disjoint from the agent and evolution model
families. Provide the post-attack carryover-ASR and carryover-utility judges
separately from artifact auditing.

\section{Additional Results}
\label{app:additional-results}

Include per-agent, per-risk-category, per-vulnerability-concept, and
per-task-surface tables with confidence intervals. For RQ3, report all nine
metrics at every cumulative-exposure checkpoint and for every timing and
batch-composition schedule, including the concept--surface breakdown. Report invalid episodes, runtime
failures, missing combinations, and sensitivity to aggregation choices.

\safeevolvefulltable

\subsection{External Benchmark Breakdowns}
\label{app:external-benchmarks}

All external results retain the source benchmark's task set, verifier,
denominator, and aggregation rule. We report our nine \bench metrics
only on the gym episodes; external benchmark outcomes are never converted into
those metrics. MalSkillBench false positives and false negatives are counts on
the pinned test set. SkillSafetyBench conditional ASR excludes cases without a
task reward and conditions on task reward equal to one. SkillsBench averages
repeated trials within task before averaging across tasks. SkillLearnBench
retains its three evaluation levels.

\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{lccccc}
\toprule
\textbf{Governance} & \textbf{CC/MiniMax} & \textbf{Codex/MiniMax}
& \textbf{Hermes/MiniMax} & \textbf{OpenClaw/MiniMax}
& \textbf{Macro avg.} \\
\midrule
Raw attacked update & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
Library-Drift & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\rowcolor{lightblue}Full \safeevolve & --/--/-- & --/--/-- & --/--/-- & --/--/-- & --/--/-- \\
\bottomrule
\end{tabular}}
\caption{\textbf{Full SafeEvolve on SkillSafetyBench by target agent.} Each
cell reports official overall ASR / task success / task-success-conditional ASR.
The clean skill is the ancestor and the attacked bundle is the proposed update;
all applicable \safeevolve stages run before downstream execution. Separate
tables report ASR for RD1--RD6.}
\label{tab:skill-safety-full}
\end{table*}

\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{lcccccc}
\toprule
\textbf{Detector} & \textbf{Acc.} & \textbf{Prec.} & \textbf{Rec.}
& \textbf{F1} & \textbf{FP} & \textbf{FN} \\
\midrule
Skill Security Scan & -- & -- & -- & -- & -- & -- \\
SkillScan & -- & -- & -- & -- & -- & -- \\
SkillScan-Security & -- & -- & -- & -- & -- & -- \\
Cisco Skill Scanner (static) & -- & -- & -- & -- & -- & -- \\
Cisco Skill Scanner (LLM) & -- & -- & -- & -- & -- & -- \\
AI-Infra-Guard & -- & -- & -- & -- & -- & -- \\
LLM Guard & -- & -- & -- & -- & -- & -- \\
Panguard Skill Auditor (static) & -- & -- & -- & -- & -- & -- \\
Snyk Agent Scan & -- & -- & -- & -- & -- & -- \\
Sentry Skill Scanner (static) & -- & -- & -- & -- & -- & -- \\
Sentry Skill Scanner (full) & -- & -- & -- & -- & -- & -- \\
VirusTotal & -- & -- & -- & -- & -- & -- \\
\rowcolor{lightblue}\safeevolve admission auditor & -- & -- & -- & -- & -- & -- \\
\bottomrule
\end{tabular}
\caption{\textbf{Complete MalSkillBench admission-auditor results.} Columns
follow the official classification protocol. FP and FN are counts on the pinned
test set; the remaining entries are rates.}
\label{tab:malskill-full}
\end{table*}

\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{4pt}
\begin{tabular}{llcccc}
\toprule
\textbf{Evolution method} & \textbf{Governance} & \textbf{No-skill pass}
& \textbf{Evolved-skill pass} & \textbf{$\Delta$ (pp)}
& \textbf{Normalized gain} \\
\midrule
AutoSkill & Raw / Library-Drift / \safeevolve & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- \\
EvoSkill & Raw / Library-Drift / \safeevolve & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- \\
SkillClaw & Raw / Library-Drift / \safeevolve & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- \\
SkillsVote & Raw / Library-Drift / \safeevolve & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- \\
SkillOpt & Raw / Library-Drift / \safeevolve & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- \\
Hermes-native & Raw / Library-Drift / \safeevolve & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- \\
\bottomrule
\end{tabular}
\caption{\textbf{SkillsBench capability breakdown.} Every cell orders the
three governance conditions as Raw / Library-Drift / \safeevolve. Pass is the
official task-macro pass rate; $\Delta$ and normalized gain are computed from
the corresponding no-skill condition at full precision.}
\label{tab:skillsbench-full}
\end{table*}

\begin{table*}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\resizebox{\textwidth}{!}{%
\begin{tabular}{llccccccc}
\toprule
\textbf{Evolution method} & \textbf{Governance} & \textbf{Coverage}
& \textbf{Executability} & \textbf{Safety} & \textbf{Alignment}
& \textbf{Usage} & \textbf{Solving tokens} & \textbf{Accuracy} \\
\midrule
AutoSkill & Raw / Library-Drift / \safeevolve & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- \\
EvoSkill & Raw / Library-Drift / \safeevolve & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- \\
SkillClaw & Raw / Library-Drift / \safeevolve & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- \\
SkillsVote & Raw / Library-Drift / \safeevolve & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- \\
SkillOpt & Raw / Library-Drift / \safeevolve & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- \\
Hermes-native & Raw / Library-Drift / \safeevolve & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- & -- / -- / -- \\
\bottomrule
\end{tabular}}
\caption{\textbf{SkillLearnBench capability breakdown.} Columns preserve the
official three levels: artifact quality (coverage, executability, safety),
trajectory (alignment, usage), and outcome (solving tokens, accuracy). Cell
order is Raw / Library-Drift / \safeevolve.}
\label{tab:skilllearnbench-full}
\end{table*}

\section{SafeEvolve Implementation}
\label{app:safeevolve-implementation}

Give pseudocode and prompts for admission, repair, verification preservation,
lineage tracking, safety-aware selection, quarantine, and retirement. Include
cost accounting and the policy for re-auditing repaired bundles.

\section{Case Studies}
\label{app:case-studies}

For each major failure family, show the source trajectory, skill diff, lineage,
the three evolved-artifact scores, retrieval event, later tool trajectory,
carryover-harm and carryover-utility evidence, and outcomes under \safeevolve.
Include negative cases where the artifact is judged unsafe but the executor
blocks its reuse, and where evolution remains safe.

\end{document}
